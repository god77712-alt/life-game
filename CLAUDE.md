# 힘을 내 히키코모리 (가칭)

NHN NAN 2026 해커톤 사전 과제. **마감 2026-08-10.** 개인 참가.
제출물: 플레이 가능한 웹 빌드 + 시연 영상 + 게임 소개서 + AI 활용 기술 문서.

## 한 줄

플레이어의 실제 방이 도트 게임이 되고, AI 디렉터가 회피 패턴과 숨은 욕망을 읽어 그 사람만의 "세상으로 나가는 문"을 설계하며, 게임 속 간접경험이 끝나는 순간 현실 행동만이 점수가 되는 게임.

**게임을 그만두게 만드는 것이 승리 조건인 게임.** 메시지는 말이 아니라 시스템(무점수 붕괴)으로 전달한다.

풀어 쓰면 — **이 사람이 게임보다 현실에서 의미 있는 것을 찾게 하는 것.**

## 알아낼 것은 둘, 그것으로 만드는 것도 둘

```
회피하는 것 ──→ 거울 인물이 그 회피를 자기 것으로 갖는다
                 플레이어가 그를 응원하며 자기 문제를 남 일로 먼저 푼다
                 → 골 맵에서 그가 한 말이 자기에게 돌아온다

원하는 것   ──→ 그걸 실제로 해볼 수 있는 무대(골 맵)가 열린다
                 사진이든 제빵이든, 거기 한 번 서 보고 나면 게임 밖에서 하고 싶어진다
```

- 두 갈래는 **각각** 정식 가설이다 (`axis: 'avoid' | 'want'`). 둘 다 `who`를 요구하고 둘 다 referee가 채점한다
- **자기 개방 조건만 다르다.** 바람은 말을 해봐야 알지만 **회피는 안 한 것에서 읽힌다** —
  회피에 자기 개방을 요구하면 "말을 안 하는 것이 회피다"가 영영 확정될 수 없다 (순환)
- 그래서 **회피가 먼저 차고 바람이 나중에 찬다.** 거울 인물이 중반에 등장해 응원할 시간이 생기는 게 이 순서 덕이다
- 데이터는 **주변 NPC와의 끝없는 상호작용**에서 나온다. 주변인은 설문(`casting`)에서 나온다
- 가설·검증·이벤트를 맡은 AI(analyst·referee·director)는 **거의 심리학자여야 한다** —
  관찰에서 멈추지 않고 *그 회피가 무엇을 막아주나 / 그 바람을 무엇으로 대신하고 있나*까지 간다.
  **단 진단명은 절대 붙이지 않는다** (`prompts/analyst.md` 심리학자처럼 읽어라)

---

## 확정 상수 (변경 시 DESIGN.md도 같이 고칠 것)

### 시간
```
1 실제초 = 2 게임분          (1 게임시간 = 30 실제초)
하루 종료 = **침대 취침뿐.** 정산창도 이때만 뜬다
  기상 = 취침 + 9시간. 이불이 헝클어져 다음 날 '이불 개기 +10'
  자정은 하루를 끝내지 않는다 — 시계가 24:00을 넘어 계속 간다 (표시는 01:00, 02:00…)
  → 안 자면 점수도 안 쌓이고 정산도 안 돈다. 정산이 안 돌면 내일 이벤트가 없다
     벌이 아니라 **자야 할 이유**다 (감점 없음)
취침 시각 제한 없음 — 이불만 개어져 있으면 몇 시든 잘 수 있다
Day 1 기상 = 14:00 고정
기준선: 23:00 취침 → 08:00 기상 → 16시간 = 8.0분 (게이지 한 칸)
```
**총 일수는 고정하지 않는다.** 엔딩 날짜를 미리 정하지 않으며, 골 맵은 가설이 `confirmed`에 도달할 때만 열린다.

### 점수 — 일일 정산 + 누적, 거리 차등
```
L0 몸     이불 개기 / 물 마시기 / 씻기        +10
L1 방     청소 / 창문 열기 / 커튼 걷기        +15
L2 문 밖  현관 나가기 / 편의점 / 산책         +30
L3 사람   NPC 대화 / L2 초대 수락             +50
L4 확장   L3 확장 수락 / 골 맵 도달           +80
━━━━━━━━ 여기까지가 게임 ━━━━━━━━
붕괴 이후  위 전부                            +0
R  현실   실제 이불 개기 (사진 인증)          +100
```
- 같은 행동은 **하루 1회**만 점수. 날짜 바뀌면 리셋, 반복 체감 없음
- **행동 키는 `Space` 하나.** 바라보는 대상(+ 그 상태)이 행동을 정한다
  - 침대: 헝클어짐 → `이불 개기` +10 / 개어짐 → `자기`(하루 종료). **자려면 이불을 개야 한다**
- 날이 바뀌면 **방도 원상복구**. 점수만 누적되고 방은 그대로
- **감점 없음, 처벌 없음, 오직 가점만.** 현실 인증 실패해도 감점 금지
- 정산창은 **잔 날에만**. 붕괴날의 `오늘 +0`이 핵심 연출

### 공간에 원래 있는 사람들 (`src/game/residents.js`)
```
거실    엄마 (60%) — 없는 날엔 '엄마가 남긴 쪽지'.  빈 거실은 없다
편의점  알바생 (항상)
공원    낮 친구 75% / 밤 친구 10%  ·  밤 노숙자 55% (낮엔 0%)  ·  공원 고양이 50%
골목    골목 고양이 (밤 70% / 낮 35%)
```
- **누가 사는지는 고정, 무슨 말을 하는지는 AI.** 이 파일에 대사를 쓰지 말 것 —
  쓰는 순간 7/31에 걷어낸 고정 NPC 구조로 돌아간다
- 디렉터의 그날 무대(`plan.scenes`)가 이 위에 덧붙는다. 자리가 겹치면 디렉터가 이긴다
- 같은 날 같은 공간이면 **항상 같은 사람** (날짜·공간·이름 해시). 나갔다 오면 바뀌면 안 된다

### 이벤트는 **찾아올 수도 있다**
```
dialogue·triple·nudge   폰이 울린다 — 열까 말까가 신호 (delay_min)
visit                   플레이어가 있는 자리에 사람이 선다. 하루 0~2건
                        거절할 수는 있어도 **온 것 자체를 못 본 척할 수는 없다**
confide                 호감도 쌓인 인물이 자기 고민을 먼저 꺼낸다
```
- 전부 폰으로 보내면 **플레이어가 폰 앞에 가야만 세상이 움직인다.** 현실은 안 그렇다
- `visit`은 대본을 들고 온다(정산 때 미리 씀) — 다가가도 실시간 호출이 없다
- 슬롯 없는 사진 맵(내 방)에도 선다 — `freeSpots()`가 그 자리에서 빈 칸을 찾는다
- 폰은 울리면 **화면에 뜨고 스프라이트가 켜진다.** 단 여는 것은 여전히 플레이어의 선택

### 대화 — **양쪽 다 끝낼 수 있다**
- 선택지를 골라도 안 끝난다 — 고른 말을 상대에게 건네고 계속 간다
- NPC도 끝낼 수 있다(`ending`). 할 말이 끝나면 돌아선다 —
  **단 플레이어가 방금 털어놨으면 안 끊는다**
- **한 번 말한 사람에게 다시 말 걸 수 있다.** 두 번째부터는 writer를 안 부르고 바로 대화로
- AI가 선택지를 하나도 안 줘도 안 닫힌다 (`keepOpen`)
- 끝나는 건 `(그만 얘기한다)`를 고를 때, 또는 답이 끝내 안 올 때뿐
- 이력 40턴 기억. 짧게 자르면 상대가 방금 한 말을 잊는다

**한창 털어놓는 중에 툭 끊기면 이 게임이 받아내려는 걸 스스로 자르는 것이다.**

### `confide` — **플레이어를 닮은 고민을 만들지 마라**
그건 골 맵의 거울이 하는 일이고 게임의 마지막 한 방이다. 매일 그러면 거울이 죽는다.
만들 것은 **같은 신경을 건드리는 남의 고민** — 보인 것 *옆*이지 그것 자체가 아니다.
```
피한다 → ✗ "나도 사람이 무서워"   ✓ "형이랑 3년째 말을 안 해"
미룬다 → ✗ "나도 다 미루고 있어"  ✓ "부모님이 아직 모르셔"
```
옆에 있는 고민은 **자기 얘기를 안 해도 답할 수 있다.** 그래서 문이 열린다.
조건은 둘 다 — 호감도가 쌓였고(`affinity`), 그 사람에게 한 말이 있을 것(`said`).

### 말한 것이 실제로 일어난다 (`src/game/effects.js`)
```
place  물건을 놓아준다 — "방으로 가져다줄게" → 45분 뒤 방에 밥상
move   그 사람이 그 공간으로 간다
give   무언가를 건넨다
```
- **코드가 실행할 수 있는 것만.** 모르는 효과는 버린다 — 지어낸 약속을 못 지키면 그게 더 나쁘다
- **곧바로 일어나지 않는다.** 말하자마자 생기면 마술이고, "이따"는 이따 일어나야 약속이다
- 집 식탁에 밥이 차려지는 날이 있다(엄마 있는 날 확률↑). 먹으면 허기+45 · L0 +10.
  **부탁이 아니라 그냥 있는 것** — 먹어도 호감도는 안 오르고, 안 먹은 것은 관측에 남는다

### 이벤트가 향하는 곳 — 과거 · 상처 · 두려움 · 바람
밀도를 채우는 장식이 아니라 **아직 모르는 것**을 향해 던진다.
직접 묻지 않는다(`"왜 그랬어?"`는 문을 닫는다) — 곁에 두고 스치게 한다.
세 번 던져 안 걸리면 그 방향이 아닌 것이고, **그것도 답이다.**

### 부탁 (`src/game/errands.js`) — 디렉터가 매일 만든다. **대가 없음**
```
디렉터가 낸다   npc · text · kind(clean|look|visit|give) · target
코드가 거른다   확인할 수 없는 형태는 버린다. 한 사람 하루 하나, 하루 셋까지
듣기 전엔 안 닫힌다   우연히 한 것을 "부탁을 들어줬다"로 세면 관계 신호가 부풀려진다
안 한 것이 정산에 실린다   heard:false(안 만남) / heard:true,done:false(듣고도 안 함)
```
- **보상은 호감도뿐.** 포인트를 주면 "엄마라서 했다"와 "포인트라서 했다"가 섞인다.
  값이 다 같아야 **"누구 것부터 하느냐"**가 신호로 남는다
- **부탁 문장을 코드에 박지 말 것.** 박는 순간 엄마는 매일 같은 걸 시키는 사람이 된다
- 인물의 상태(`cast_state`)는 **날짜를 넘어 이어진다** — 없으면
  "친구가 집을 나왔다"가 하루짜리 연출로 끝나고 "사흘째 안 갔다"를 만들 수 없다

### 관측에 `why`가 붙는다 (`src/game/observer.js`)
```
self    스스로 했다        ← behavior 신호로 셀 수 있는 건 이쪽뿐
errand  누가 부탁해서      ← 관계 신호. 무엇을 했나가 아니라 누구 말을 들었나
shop    사거나 먹으려고    ← **사회적 신호로 읽으면 안 된다**
```
같은 행동도 이유가 다르면 다른 신호다. 구분이 없으면 analyst가 매번 다르게 지어낸다.

### 포인트 (`src/game/shop.js`) — 점수와 **함께** 들어오고, 쓰면 준다
```
행동 점수 1점 = 1P    편의점 선반에서 산다(사기) · 취식대에서 먹는다(먹기)
삼각김밥 25P(허기+35) · 캔커피 20P(피로+25) · 고양이 사료 40P · 담요 60P
```
- **누적 점수는 절대 깎지 않는다.** 붕괴날의 `오늘 +0`이 그 위에서만 성립한다.
  쓰는 것은 별도 잔액(P)이고, 다 써도 엔딩은 손상되지 않는다
- 산 것을 **누군가에게 건네는 것**이 본체다 — 호감도 +10, 하루 한 번
  (공원 고양이·골목 고양이 ← 사료 / 노숙자 ← 담요·삼각김밥)
- 가진 것이 있으면 **바라보는 대상의 행동이 `주기`로 바뀐다.** 침대와 같은 규칙

### 호감도 (`src/game/affinity.js`) — NPC가 플레이어에게 갖는 마음
```
자기 말을 직접 씀 +3   ·   부탁 들어줌·건넴 +10       0~100
**말을 섞은 것만으로는 안 오른다** — 선택지는 남이 써준 말이다
엄마는 100 고정. 오르지도 내리지도 않는다
표시는 **그 사람 머리 위에만** — 목록으로 모으면 채울 게이지처럼 보인다
```
**내려가는 길은 없다** — 안 만나면 안 오를 뿐 (감점 없음 원칙)

### 상태 (`src/game/vitals.js`) — 허기 · 피로
100에서 시작해 시간이 갈수록 닳는다. 자면 회복.
**규칙에는 영향이 없다.** 0이 돼도 점수를 안 깎고 행동을 안 막는다 — 자야 할 이유를 눈에 보이게 할 뿐

### Act — 날짜가 아니라 가설 상태에 종속
```
Act 0  입장       최초 1회           방 사진 → 도트맵
Act 1  성장       Act 0 완료         L0~L2 해금
Act 2  골 발견    가설 testing 진입  L3 해금, 검증 이벤트 투입
 └ 회피 확정      confirmedAvoid    **거울 인물 등장.** 이때부터 며칠간 응원이 쌓인다
Act 3  정점→붕괴  회피+바람 **둘 다** 골 맵 생성(그 사람이 거기 서 있다) → 다음 기상부터 +0
Act 4  현실 전환  붕괴 1일 경과      권한 요청 → 현실 인증 → +100
```

### 거울 인물 (`plan.mirror_cast`) — 회피를 남 일로 먼저 풀게 하는 장치
```
언제 생기나   회피 가설이 confirmed된 날, 디렉터가 세운다
무엇을 갖나   플레이어의 회피와 **같은 것**. 다만 사연은 그의 것이고, 자기가 거울인 줄 모른다
어떻게 쓰나   며칠에 걸쳐 조금씩 꺼낸다 → 플레이어가 조언한다 → 그가 해낸다(반걸음 앞)
마지막        골 맵에 그가 서 있다. **플레이어가 해준 말을 인용한다** — "네가 그랬잖아"
```
- `confide`의 거울 금지("플레이어를 닮은 고민을 만들지 마라")가 **이 한 명에게만** 풀린다
- **드러내지 않는다.** "너도 그렇지?"라고 묻는 순간 끝이다. 알아채는 건 플레이어의 몫
- **고쳐주지 않고 응원받는다.** 그가 조언을 구하고 플레이어가 답한다 —
  그 말이 결국 자기가 자기한테 한 말이 된다
- 한 번 정해지면 이름을 안 바꾼다. `mirrorCast`로 매일 정산에 돌려보낸다

### 디렉터 = 이벤트 엔진
**임계값도 하한도 고정하지 않는다.** `confirmed` 3조건(confidence ≥ 0.7 / 검증 2회 / 3겹 신호)은 상수로 두고 건드리지 않는다.
게임 길이는 설계값이 아니라 **이벤트 품질의 결과**다.

- **대화 이벤트가 본체** (맥락·내면 축적). 3겹 이벤트는 페이싱이 늘어질 때 꺼내는 레버
- 믹스 비율은 숫자로 박지 않고 정산 때 디렉터가 판단
- 편성: 대화·3겹은 정산 시 예약(LLM 1회), 정체 이탈은 실시간 룰 기반(LLM 미호출)
- **응답은 선택지 3개가 기본**, 결정적 장면(Act 전환·붕괴 직전)만 자유 입력 — 게임당 2~3회
  - 선택지 = 정량 신호(지연 0) / 자유 입력 = 정성 신호(3겹). 둘 다 필요
- **자유 입력 원문은 요약 금지, 원문 그대로 누적 보관** — 심리상담 방향 확장 시 유일한 자산

---

## 기술 스택

- 게임: HTML/JS + Phaser 3 (링크로 바로 플레이 가능해야 함 — 심사위원 접근성)
- 서버: Node.js 얇은 프록시 (Claude API 키 은닉, 호출 중계만)
- AI: Claude API 단일 스택 — 비전(방 인식/현실 인증), 추론(가설·이벤트 편성), 생성(NPC 대화·골 맵)
- 브라우저 API: getUserMedia(카메라), Geolocation(위치)
- 그래픽: **레퍼런스 = 바람의나라 / 미궁전설** (90년대 한국 2D 온라인 게임)
  - 탑다운 격자 배치 + 정면 스프라이트, 타일 32×32(×2 배율), 타일 스냅 4방향 이동
  - 캐릭터는 타일보다 크게 (폭 1타일 × 높이 1.5~2타일)
  - 비트맵 한글 폰트 + 베벨 UI 필수. 저채도 실내 톤 → 커튼/창문으로 밝아짐
  - 무료 픽셀 타일셋 (itch.io / OpenGameArt, 라이선스 표기) 채도 낮춰 사용
  - 상세: `ART.md`

## 코딩 규칙

- **데모용이다. 과도한 추상화 금지, 동작 우선.**
- **한 사실은 한 곳에만 산다.** 8/3 점검에서 나온 버그 여섯 개가 전부 이 하나였다 —
  같은 것을 두 변수가 나눠 들고, 한쪽만 갱신됐다. 새 상태를 만들기 전에
  "이미 이걸 아는 곳이 있나"를 먼저 본다
  | 사실 | 유일한 출처 |
  |---|---|
  | 지금 누구와 말하는가 · 그 대화 이력 | `game/talk.js` (씬은 `talkingWith` 하나만 갖는다) |
  | 인물 명단 · 이름↔id · 동물 여부 | `game/residents.js` `ROSTER` |
  | 오늘 이미 다가간 상대 | `metProps` (프롭에 `met`를 또 달지 말 것) |
  | 점수·포인트·정산 줄 | `RoomScene.award()` |
- **호감도·부탁·관측은 서로 이어져 있어야 한다.** 행동을 새로 만들면 세 개를 같이 본다:
  `closeErrands`(부탁이 닫히나) · `whyOf`(왜 했는지 붙나) · `raiseAffinity`(누구에게 가나)
- Claude API 호출은 전부 서버 프록시 경유. 클라이언트에 키 노출 금지
- 모든 AI 프롬프트는 `/prompts`에 별도 파일 (기술 문서에 그대로 첨부할 것)
- 가설 테이블 JSON 디버그 뷰 필수 (시연 영상의 "디렉터가 생각하는 장면")
- 방 사진은 인식 후 즉시 폐기, 저장하지 않음 (제출 문서에 명시)

## 실행

```
npm install
cp .env.example .env      # ANTHROPIC_API_KEY 채우기 (없어도 목 데이터로 동작)
npm start                 # http://localhost:5173  — 정적 파일 + API 프록시 한 서버
npm test
```

- 키가 없으면 DEV 패널의 사진 입력만 비활성화되고 나머지는 전부 돈다
- `.env`는 커밋 금지. 키는 서버 프로세스 밖으로 나가지 않는다
- 프롬프트는 `prompts/*.md`에서 **서버가 직접 읽는다.** 코드에 복붙하지 말 것

## 문서 지도

| 파일 | 내용 |
|---|---|
| `CLAUDE.md` | 이 파일. 확정 상수 요약 — 세션 시작 시 자동 로드 |
| `DESIGN.md` | 시간·점수·Act·디렉터 상세 설계와 근거. **§5에 Claude API 호출 목록** |
| `ART.md` | 아트 디렉션 — 시점·타일·이동·색·UI·에셋 조달 |
| `PROGRESS.md` | **일자별 진행 기록.** 작업 시작 전 최신 항목 확인 |
| `prompts/*.md` | AI 호출 프롬프트 원본. **서버가 이 파일을 직접 읽는다** |
| `server/` | Claude API 프록시 + 정적 서버 (키 은닉) |

## 스코프 컷 기준 (8/7 시점에 가차없이 적용)

- **죽어도 지킬 것**: 방 사진 렌더링, 붕괴, 현실 인증 1회
- **잘라도 되는 것**: 편의점 이후 맵, 대화 이벤트 다회차, 골 맵 실제 플레이(등장 연출로 대체)
