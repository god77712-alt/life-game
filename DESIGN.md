# 코어 시스템 확정 — 시간 · 점수 · Act

기획서(`CLAUDE_5.md`)의 빈칸을 메운 문서. 이 3개가 나머지 모든 구현의 전제다.

---

## 1. 시간

### 환산율

```
1 실제초 = 2 게임분
1 게임시간 = 30 실제초
```

### 하루의 시작과 끝

**하루 종료 = 침대 취침뿐이다.** 정산창도 이때만 뜬다.

> **뒤집은 것 (8/2).** 예전에는 자정도 하루를 끝냈다. 그러면 안 자고 버틴 날에도
> 정산이 돌아서 **"잤다"와 "안 잤다"가 사실상 같아진다** — 자정까지 버티는 쪽이
> 하루가 더 길기까지 하니 안 자는 게 이득이었다.
> 이제 자정은 그냥 지나가는 시각이고, 시계는 24:00을 넘어 계속 흐른다(표시는 01:00, 02:00…).

| | 자면 | 안 자면 |
|---|---|---|
| 시간 | 취침 + 9시간에 기상 | 계속 흐른다 (25:00, 26:00…) |
| 점수 | 정산되어 누적에 들어간다 | 안 쌓인다 |
| 디렉터 | 정산이 돌아 **내일 이벤트가 생긴다** | 안 돈다 → 내일도 빈 하루 |
| 이불 | 헝클어짐 → **이불 개기 +10** | 개어진 그대로 → 기회 없음 |

**벌이 아니라 자야 할 이유다.** 감점은 여전히 없다(§2 원칙) — 안 자면 잃는 게 아니라 안 얻는다.
허기·피로 게이지가 닳는 것을 눈으로 보게 해서 이 압력을 말 대신 화면으로 전한다.

> **취침 시각 제한은 없앴다 (8/2).** 이불만 개어져 있으면 몇 시든 잘 수 있다.
> 예전에는 20:00 이전을 막았다 — Day 1 기상 직후(14:00)에 자면 기상이 23:00이라
> 다음 날이 1시간짜리가 되기 때문이었다. 지금도 그렇게 되지만 **막지 않는다.**
> 짧은 하루를 살지는 플레이어가 정한다.

하루가 끝나면 정산창 → 디렉터 가설 갱신 → 다음 날.

### 하루 길이 표

| 취침 | 기상 | 활동 | 실제 시간 |
|---|---|---|---|
| 20:00 | 05:00 | 19시간 | 9.5분 |
| 21:00 | 06:00 | 18시간 | 9.0분 |
| 22:00 | 07:00 | 17시간 | 8.5분 |
| **23:00** | **08:00** | **16시간** | **8.0분** (기준선) |
| 24:00 | 09:00 | 15시간 | 7.5분 |

- Day 1 기상은 전날이 없으므로 고정: **14:00** (10시간 = 5분). 늦잠으로 시작해 하루가 짧은 게 첫 학습 신호.

### 설계 의도와 실제 동작

취침 시각을 일정하게 유지하면 활동 시간은 **항상 15시간(7.5분)으로 수렴**한다 (24h = 수면 9h + 활동 15h).
따라서 이 시스템은 "일찍 자면 이득"이 아니라 **오늘을 줄여 내일을 사는 재분배**다.

- 자정 무렵에 자는 플레이어 → 매일 7.5분 고정
- 자발적으로 일찍 자는 플레이어 → 그날은 짧고 다음 날이 김

감점은 없다. 기회의 총량이 재배치될 뿐.

### 총 일수

**고정하지 않는다.** 엔딩 날짜는 미리 정하지 않으며, 골 맵은 디렉터의 가설이 `confirmed`에 도달했을 때만 열린다.
따라서 총 플레이 일수는 설계값이 아니라 **플레이어마다 다른 결과값**이다.

---

## 2. 점수

### 구조: 일일 정산 + 누적

매일 **취침 시** 정산창이 뜬다. 이 화면이 게임의 리듬 단위이자 붕괴 연출의 무대다.

```
Day 1  정산   오늘 +120     누적  120
Day 2  정산   오늘 +140     누적  260
Day 3  정산   오늘 +150     누적  410
  ⋮                            (일수 유동)
Day N  정산   오늘 +210     누적  890   ← 역대 최고 일일점
붕괴날 정산   오늘   +0     누적  890   ← 정산창이 곧 연출
현실인증      오늘 +100     누적  990
```

매일 보던 화면이기 때문에 `+0`을 몰라볼 수 없다. 일수가 유동적이어도 "오늘"은 항상 비교 가능하다.

### 행동별 점수: 거리 차등

방에서 멀어질수록 크게. 점수표 자체가 "나가라"는 메시지가 된다.

| 계층 | 행동 | 점수 |
|---|---|---|
| L0 몸 | 이불 개기 / 물 마시기 / 씻기 | +10 |
| L1 방 | 청소 / 창문 열기 / 커튼 걷기 | +15 |
| L2 문 밖 | 현관 나가기 / 편의점 / 산책 | +30 |
| L3 사람 | NPC 대화 / L2 초대 수락 | +50 |
| L4 확장 | L3 확장 수락 / 골 맵 도달 | +80 |
| — | **여기까지가 게임** | |
| 붕괴 이후 | 위 전부 | **+0** |
| R 현실 | 실제 이불 개기 (사진 인증) | **+100** |

`10 → 15 → 30 → 50 → 80 → 100`. 현실 +100이 뜬금없는 숫자가 아니라 곡선의 끝점으로 읽힌다.
새 상호작용을 만들 때 "이건 몇 점?"이 자동으로 정해지는 것도 이 표의 목적이다.

### 규칙

- **행동 키는 하나.** 바라보는 대상이 행동을 정한다 (`ART.md` §3-1). 메뉴·커서·행동별 단축키 없음
- **침대만 상태로 행동이 갈린다.** 헝클어진 이불 → `이불 개기` L0 +10 / 개어진 이불 → `자기`(하루 종료).
  키가 하나뿐이라 "무엇을 보느냐"에 "그게 어떤 상태냐"를 더했다. 결과적으로 **자려면 이불을 개야 한다**
- **날이 바뀌면 방도 원상복구된다.** 쓰레기가 다시 생기고 커튼이 다시 쳐지고 이불이 헝클어진다.
  점수만 누적되고 방은 그대로 — 아무것도 변하지 않는 반복이 이 게임의 배경이다
- 같은 행동은 **하루 1회**만 점수가 된다 (이미 갠 이불을 다시 갤 수 없음).
  판정 단위는 **오브젝트 id** — 쓰레기 더미가 3개면 기회도 3번
- `window`/`curtain`은 bed·door와 함께 **필수 주입**. 하나도 없으면 L1 밝기 행동이 사라진다
- 날짜가 바뀌면 리셋. 반복 체감은 적용하지 않음
- **감점 없음, 처벌 없음, 오직 가점만.** 현실 인증 실패해도 감점 금지
- L2 이상 계층은 Act 진행에 따라 순차 해금

---

## 3. Act 상태 머신

Act는 **날짜가 아니라 디렉터의 가설 상태에 종속**된다. 날짜는 흘러가는 배경일 뿐이다.

| Act | 진입 조건 | 내용 | 해금 |
|---|---|---|---|
| **0** 입장 | 최초 1회 | 카메라 권한 → 방 사진 → 도트맵 렌더링 | — |
| **1** 성장 | Act 0 완료 | 방 안 상호작용, 방문 열면 편의점 | L0~L2 |
| **2** 골 발견 | 가설 하나가 `testing` 진입 | L1 노출 → L2 초대 검증 이벤트 투입 | L3 |
| ↳ 회피 확정 | `confirmedAvoid` | **거울 인물 등장.** 여기서부터 며칠간 응원이 쌓인다 | — |
| **3** 정점→붕괴 | 회피·바람 **둘 다** 확정 | 골 맵 생성(거울 인물이 거기 서 있다) → **다음 기상부터 전 행동 +0** | L4 |
| **4** 현실 전환 | 붕괴 1일 경과 | 카메라/위치 권한 → 현실 인증 → +100 | R |

### 왜 갈래를 둘로 나눴나 (8/3)

이 게임이 알아내야 할 것은 둘이고, **각각이 만드는 것이 다르다.**

```
avoid  무엇으로부터 자기를 지키는가  →  거울 인물이 그 회피를 자기 것으로 갖는다
want   무엇을 해보고 싶은가         →  골 맵이 그 무대로 열린다
```

그 전까지 가설은 한 갈래(`who`/`through`/`when` — 입이 열리는 조건)뿐이었고,
회피는 `avoidance.pattern` 한 문장으로 **검증 없이** 떠 있었으며, 바람은
8/1에 `desire`를 걷어내면서 **통째로 사라졌다.** 그래서 골 맵이 검증을 안 거친
"자기 입으로 한 말"에서 바로 지어졌다.

8/1의 진단이 반쪽이었다. '고양이에게 밥 주기'가 confirmed된 건 욕망을 물어서가 아니라
**상대 없는 욕망**을 물어서였다. `who`를 필수로 두고 욕망을 되살렸어야 했다.

**자기 개방 조건이 갈래마다 다른 이유** (`server/hypothesis.mjs` `AXES`):
바람은 그 사람에게 말을 해봐야 알 수 있지만, 회피는 **안 한 것**에서 읽힌다 —
안 갔고, 안 열었고, 화제를 돌렸다. 회피에 자기 개방을 요구하면
"말을 안 하는 것이 회피다"라는 가설이 영영 확정될 수 없다. 그건 논리가 아니라 순환이다.

그 결과 **회피가 먼저 차고 바람이 나중에 찬다.** 거울 인물이 중반에 등장해
응원할 시간이 생기는 것이 이 순서의 효과다 — 한 번 만난 사람은 응원할 수 없다.

### 붕괴 구간

- 기획서 원안은 "무점수 최소 2~3분". 일 단위로 옮기면 **붕괴 1일 = 약 7.5분**으로 원안보다 길다. 1일이면 충분.
- 그날 밤 정산에서 `오늘 +0`이 한 번 뜬다. 이 한 장면이 붕괴의 핵심 연출.
- 배경음 제거는 붕괴 기상 시점부터.

---

## 4. 디렉터 = 이벤트 엔진

### 원칙: 숫자를 조이지 않고 이벤트로 민다

**`confirmed` 임계값도, 회피형 플레이어의 하한도 고정하지 않는다.**
원안 3조건은 상수로 두고 건드리지 않는다.

```
confidence ≥ 0.7
검증 이벤트 2회 이상 통과
3겹 신호(언어·반응·행동) 모두 존재
```

대신 디렉터가 두 방향으로 이벤트를 만든다.

1. **신호 획득 이벤트** — 임계값을 빠르게 당겨온다
2. **정체 이탈 이벤트** — 플레이어가 하한에 머물지 않게 한다

따라서 게임 길이는 설계값이 아니라 **이벤트 품질의 결과**다. 밸런싱 대상은 숫자가 아니라 이벤트 카탈로그.

### 대화와 3겹은 우열이 아니라 역할이 다르다

3겹 이벤트가 "좋은 이벤트"인 게 아니다. **둘은 목적이 다르다.**

| | 대화 이벤트 | 3겹 이벤트 |
|---|---|---|
| 뽑는 신호 | 언어 (얕음) | 언어 + 반응 + 행동 |
| 남기는 것 | **플레이어의 맥락·내면** | confidence 수치 |
| 역할 | 이 게임의 본체 | 페이싱 조절 레버 |
| 예시 | NPC끼리 떠들다 가벼운 공 넘기기 | "편의점에 새 도시락 나왔대" → 응답 → 재료 코너 체류 |

**대화는 신호 수집의 비효율적 수단이 아니라 그 자체가 목적이다.**
이 게임은 장기적으로 심리상담에 가까운 방향으로 확장될 수 있고, 그 토대는 3겹 이벤트가 아니라 쌓인 대화다.
3겹만 던지는 디렉터는 빠르지만 플레이어를 모른다.

**3겹 이벤트 구조 (참고)**

```
NPC  "편의점에 새 도시락 나왔대"
  ├ 언어 신호   응답 내용에 요리 관련 어휘가 섞이는가
  ├ 반응 신호   응답 속도·길이가 다른 화제 대비 어떤가
  └ 행동 신호   이후 편의점 재료 코너 체류 40초
```

### 믹스 비율은 상수가 아니라 디렉터의 판단

기본값은 **대화 중심**이다. 3겹은 페이싱이 늘어질 때 꺼내는 카드다.

```
순조로움          대화 위주, 3겹 0~1개
게임이 길어짐     3겹 비중 점진 증가
정체가 뚜렷함     3겹 위주 + 정체 이탈 이벤트 병행
```

이 비율을 숫자로 박지 않는다. 정산 시점에 디렉터가 경과 일수·confidence 추이·행동 신호 유무를 보고 직접 정한다.
편성 프롬프트(`/prompts`)에 판단 기준을 문장으로 준다.

### 플레이어 응답: 선택지 기본, 결정적 장면만 자유 입력

```
평소          선택지 3개        정산 때 대사와 함께 미리 생성 → 실시간 호출 0
결정적 장면   자유 입력         게임당 2~3회. Act 전환·붕괴 직전 등
```

두 경로에서 나오는 신호의 성격이 다르고, 둘 다 필요하다.

| | 선택지 | 자유 입력 |
|---|---|---|
| 신호 | **정량** — 선택지 ID를 가설 테이블에 바로 가산 | **정성** — 어휘·길이·응답 속도 3겹 |
| 강점 | confidence 계산이 깔끔, 지연 없음 | 플레이어가 실제로 쓴 말 |
| 비용 | 없음 (예약분에 포함) | 실시간 호출 1회 |

선택지만으로는 플레이어를 모르고, 자유 입력만으로는 매 턴 지연이 붙는다.
**정량으로 가설을 굴리고, 결정적 순간에만 정성으로 확인한다.**

> 확장 대비: **자유 입력 응답은 반드시 원문 그대로 누적 보관한다.** 요약 금지.
> 선택지는 선택 기록(ID + 시각)만 남기면 되지만, 자유 입력 원문은 심리상담 방향으로 갈 때
> 유일한 자산이 된다. 게임당 2~3개뿐이라 용량 부담도 없다. 지금 버리면 나중에 복구 불가.

### 편성 방식: 하이브리드

**【정산 시점 — 밤】** LLM 호출 1회

- 가설 테이블 갱신
- 내일 투입할 **신호 획득 이벤트** 1~2개 편성, 발화 시각까지 지정
  - `10:30` 엄마 "밥 문 앞에 둔다"
  - `15:00` TV에서 요리방송 (L1 노출)

**【플레이 중 — 실시간】** 룰 기반, LLM 미호출

- 무행동 / 침대 주변 배회 감지
- **정체 이탈 이벤트** 즉시 발생 (예약과 별개)
  - 노크 / 진동 / 창밖 소리

LLM 호출이 정산 때 1번으로 몰려서 비용·지연 관리가 쉽고, 정체 이탈만 즉각 반응한다.

### 이벤트 계열 정리

| 계열 | 트리거 | 목적 | 강도 점증 |
|---|---|---|---|
| 대화 | 정산 시 예약 | 맥락·내면 축적 (기본값) | — |
| 3겹 | 정산 시 예약 | confidence 가속 (페이싱 레버) | L1 노출 → L2 초대 → L3 확장 |
| 정체 이탈 | 실시간 감지 | 하한 탈출 | 엄마 밥 → 문 앞 밥상 → 친구 노크 |

두 계열 모두 **거절 가능, 무벌점**. 기획서의 "처벌 없음, 가점만" 원칙을 그대로 따른다.

---

## 5. AI 구성 — 역할별로 나눈다

하나의 큰 AI가 아니라 **역할이 다른 AI 여러 개**를 등록해 쓴다 (`server/agents.mjs`).

### 왜 나누는가

분석과 생성은 요구가 정반대다.

| | 분석 (`analyst` · `director`) | 생성 (`writer`) |
|---|---|---|
| 스키마 | 고정. 흔들리면 안 됨 | 최소 |
| 창의성 | 불필요. 일관성이 전부 | 필수. 매번 달라야 함 |
| effort | `high` | `medium` |

합치면 JSON을 강제하는 순간 대사가 딱딱해지고, 대사에 집중하면 스키마가 흔들린다.

### 등록된 AI

| # | AI | 역할 | 시점 | 빈도 | 프롬프트 |
|---|---|---|---|---|---|
| 0 | `casting` | **캐스팅** — 설문 → 세계·등장인물 | Act 0 | 게임당 1회 | `prompts/casting.md` |
| 1 | `room-vision` | 방 사진 → 객체 JSON | Act 0 | 게임당 1회 | `prompts/room-vision.md` |
| 2 | `analyst` | **사용자 분석** — 회피 패턴·숨은 욕망 읽기 | 정산 | 하루 1회 | `prompts/analyst.md` |
| 3 | `director` | **편성** — 내일 무엇을 언제 왜 | 정산 | 하루 1회 | `prompts/director.md` |
| 4 | `writer` | **집필** — 편성 1건 → 대사·선택지 | 정산 | 하루 1~2회 | `prompts/writer.md` |
| 5 | `npc-reply` | 자유 입력 응답 | 결정적 장면 | 게임당 2~3회 | ⬜ |
| 6 | `goal-map` | 골 맵 생성 | `confirmed` | 게임당 1회 | ⬜ |
| 7 | `reality-check` | 현실 인증 사진 검증 | Act 4 | 게임당 1회 | ⬜ |

**새 AI 추가 = 프롬프트 md 하나 + `AGENTS`에 한 줄.** 실행기·폴백·검증·usage 집계는 공통.

### 시작 설문 — 무대를 세우되 가설은 세우지 않는다

게임 시작 전 9문항. **전부 일상 질문의 얼굴을 하고 있다.**

```
1  성별을 물어도 될까
2  지금 몇 살쯤 되었나
3  요즘 뭐 하고 지내
4  하루 중 제일 마음이 편한 시간은        → 깨어 있고 반응할 수 있는 구간
5  집 근처에서 제일 자주 지나는 곳은      → 방 밖으로 나가는 통로
6  휴대폰 알림은                        → 말이 닿는 경로가 열려 있는가
7  혼자 있을 때 소리는                   → 고립의 질감
8  영상 볼 때 끝까지 보는 건              → 주의가 향하는 방향
9  몇 살의 나로 살아볼까                 ← 이 설문의 본체
```

### 의도가 보이면 안 된다

> ~~"그래도 밖에 나갈 일이 있다면"~~ → **"집 근처에서 제일 자주 지나는 곳은"**
> ~~"연락이 오면 그나마 확인하는 쪽은"~~ → **"휴대폰 알림은"**

앞의 것들은 **"너는 방에서 안 나온다"를 전제로 깔고** 묻는다. 전제가 보이는 순간
플레이어는 방어적으로 답하고, 게임이 시작되기도 전에 낙인이 찍힌다.

누구나 답할 수 있는 질문으로 묻고, **답이 신호가 되게** 한다.
`알림 다 꺼둠` 하나로 캐스팅이 이렇게 쓴다:

> *"말은 도착해 있지만 울리지 않는다. 본인이 화면을 열 때만 한꺼번에 닿는다.
> 즉시 닿는 경로는 방문 두드리는 소리와 문 앞에 놓인 것뿐"*

| 설문이 정하는 것 | 설문이 정하지 않는 것 |
|---|---|
| 누가 존재하는가 (엄마·형·동기·주민센터) | 무엇에 끌리는가 |
| 어떤 압력이 있는가 (입시 / 취업 / 부모의 나이) | 무엇을 피하는가 |
| **언제 · 어디로 · 누구를 통해 닿는가** | 가설 테이블 |
| 계절, 방 밖의 세계 | |

**경계는 "무엇"과 "언제·어디로·누구를 통해"다.** 무엇에 끌리는지는 끝까지 묻지 않는다.
접근 조건은 무대의 일부이고, 이걸 모르면 디렉터가 시각과 통로를 **추측**한다.

```
awake_window   "실질 각성 15:00~06:00. 활동 집중 23:00~05:00. 07:00~14:00 취침"
outside_route  "새벽 2~4시, 도보 5분 편의점. 낮에는 나가지 않는다"
channel        "① 어머니 — 문 앞 쪽지  ② 카톡(정민) — 읽고 답 안 함이 기본
                ③ 전화(큰누나) — 대개 안 받음  ④ 아버지 — 말이 아닌 소리로만"
```

**시각이 이 게임의 자원이다**(하루 = 실제 7~9분). 깨어 있지 않은 시각에 던진 이벤트는
하루를 통째로 버린다. 4~6번이 `confirmed`까지의 일수를 가장 크게 줄인다.

### texture — 어디를 먼저 건드려볼까

7·8번은 기질의 결을 준다. 캐스팅이 `texture` 한 문단으로 정리해 **디렉터에게만** 넘긴다.

> *"영상은 결과보다 만드는 과정을 끝까지 본다. 손이 움직이는 것, 순서가 있는 것,
> 완성 안 해도 되는 것부터 건드려볼 만하다. 단, 이건 시작점 후보일 뿐 취향으로 확정하지 말 것."*

**texture는 가설이 아니다.** 디렉터가 초반에 어느 쪽을 먼저 떠볼지 정하는 데만 쓴다.
분석 AI는 texture를 보지 않는다 — 근거는 오직 관측된 행동이다.
**texture와 행동이 어긋나면 행동이 맞다.**

---

## 5-1. 공간 — 방 하나 + 고정 맵 5개

방만 사진에서 생성된다. 나머지는 손으로 쓴 레이아웃이지만 **같은 vision JSON 형식**이라
`buildRoom()`을 그대로 통과한다. 매퍼가 하나면 된다.

```
        내 방  ← 사진에서 생성
          │ (방문)
        거실
          │
        현관
          │
        골목 ──── 편의점
          │
        공원
```

| 규칙 | |
|---|---|
| 점수 | **공간 도착 = L2 +30, 공간마다 하루 1회.** 문이 아니라 도착에 붙인다 |
| 재방문 | 0점. 문에 점수를 붙이면 왔다 갔다 하며 긁을 수 있다 |
| 도착 지점 | 돌아오는 문 앞. 들어온 방향이 몸에 남는다 |
| 상태 보존 | 치운 쓰레기·갠 이불·열린 창문은 **맵을 떠났다 와도 유지**된다 (`mapState`) |
| 하루 1회 | 판정 키가 **`맵:오브젝트id`**. 방 쓰레기를 치웠다고 현관 쓰레기가 잠기면 안 된다 |
| 밝기 | 집 안에서 창문/커튼을 하나라도 열면 **집 전체가** 밝아진다. 실외는 항상 밝다 |
| 하루 시작 | 항상 자기 방. `visited` · `mapState` · `houseLit` 전부 리셋 |
| 검증 | 손으로 쓴 맵은 `validateVision`을 거치지 않는다 — 거치면 문이 하나로 합쳐진다 |

**늘려도 된다.** `MAPS`에 항목 하나 + `exits` 연결이면 끝이고,
연결이 끊기거나 고립된 맵이 생기면 테스트가 잡는다.

**이 경계가 이 게임의 전제다.** 설문에서 욕망을 받아버리면 디렉터는 관찰을 그만두고
설문을 재생하기만 한다. `casting`의 출력 스키마에는 욕망·회피 필드가 아예 없다.

**나이가 압력의 종류를 바꾼다.** 그리고 올라갈수록 커지지 않고 **조용해진다.**

```
19세  재수학원 등록만 해둠. 3월에 다들 출발했는데 이 방만 2월에 멈춰 있다
      아빠가 유일하게 소리 내어 다그치는데, 그 횟수가 줄어드는 게 더 무섭다
41세  4년째. 어머니 69세의 느려진 발소리가 압력. 형이 무릎 수술 얘기를 꺼낸다
      주민센터가 고립가구 확인차 다녀간다 — 행정이 이 집을 분류했다는 신호
```

**실제 나이와 플레이 나이의 간극 자체가 신호다.** 과거를 골랐는가, 미래를 골랐는가.
단 `gap_note`는 **메모일 뿐 가설이 아니다** — 행동으로 확인되기 전까지 서사에 반영 금지.

### 관측 — 안 한 것을 기록한다

분석 AI는 하나면 된다(`analyst`). 가설 테이블이 하나인데 갱신하는 AI가 둘이면 충돌한다.
부족했던 건 AI가 아니라 **데이터**였다 — 지금까지 analyst는 *한 행동*만 봤다.

회피형인 사람을 읽는 데 가장 강한 증거는 **안 한 것**이다. `src/game/observer.js`가 기록한다.

| | 무엇을 뜻하는가 |
|---|---|
| `engaged` | 실제로 한 일. `gazed_sec`이 길면 망설이다 한 것 |
| `passed` | **바라봤는데 하지 않았다.** 끌림과 두려움이 같이 있는 자리 |
| `unseen` | 같은 공간에 있었는데 다가가지도 않았다 |
| `places` | 어디에 얼마나 머물렀나. 짧게 들렀다 나온 곳은 회피의 흔적 |
| `notifications` | `delay_min` — **이게 reaction 신호다** |

```
한 것        내 방의 쓰레기 (2초 보고)
지나친 것    내 방의 컵  3.4초        ← 바라보고 그냥 갔다
안 다가감    창문 · 침대 · 옷더미
체류         내 방 14:00~15:00  60분
```

`reaction` 신호가 계속 비어 있던 이유가 이것이다. 강제로 열리는 대화에서는
"얼마나 지나서 응했는가"를 잴 수 없다. 응시 시간과 알림 지연이 그 자리를 메운다.

### 문턱 판정은 코드가 한다

`confirmed` 3조건은 상수다(§4). 모델에게 맡기면 매 호출마다 흔들린다.
**`analyst`는 근거(신호·confidence)만 내고, `server/hypothesis.mjs`가 결정론적으로 판정한다.**
Act 번호도 여기서 나온다 — 날짜가 아니라 가설 상태에 종속(§3).

### 정산 파이프라인

```
analyst ──▶ hypothesis.mjs (status·Act 판정) ──▶ director ──┬──▶ writer  (이벤트 1)
  분석              코드                          편성       └──▶ writer  (이벤트 2)   ← 병렬
```

**실측 (DAY 3, 이벤트 2건):**

| AI | 시간 | 입력 | 출력 |
|---|---|---|---|
| analyst | 28s | 2337 | 1523 |
| director | 38s | 3537 | 2013 |
| writer ×2 | 13s (병렬) | 3534 | 848 |
| **합계** | **79s** | **9408** | **4384** |

**하루 정산 = $0.157.** 10일 완주 + 비전 2회 ≈ **$1.7**.

### 토큰을 아끼는 법

**개발 중 반복 호출이 실제 플레이보다 훨씬 비싸다.** 프롬프트를 다듬는 동안
입력이 거의 같은 호출을 수십 번 돌리게 된다. 순서대로 효과가 크다.

| 방법 | 효과 | 상태 |
|---|---|---|
| **응답 캐시** (`server/cache.mjs`) | 개발 중 반복 호출 **→ $0** | ✅ |
| **프롬프트 캐싱** (system에 `cache_control`) | 반복 입력 **→ 0.1배** | ✅ |
| `effort` 낮추기 (director `high`→`medium`) | 출력 토큰 절반 → **하루 −$0.05** | ⬜ 측정 필요 |
| 모델 전환 (writer·npc-reply → Sonnet 5) | 그 호출 **−60%** (인트로가 8/31까지) | ⬜ 결정 필요 |
| `reasoning` 길이 제한 | 출력 감소. 단 **디버그 뷰 자산**이라 신중 | ⬜ |

캐시 키에 **프롬프트 본문·스키마·모델·effort**가 전부 들어간다 —
한 글자만 고쳐도 자동으로 무효화되므로 낡은 응답을 볼 일이 없다.
끄려면 `AI_CACHE=0`, 한 건만 새로 부르려면 요청에 `x-no-cache`.

> ⚠️ **직접 호출은 돈이 든다.** 개발 중에는 DEV 패널의 `대화 즉시` · `목 이벤트 예약`을
> 먼저 쓰고, `정산 실행`은 실제로 확인이 필요할 때만 누른다.

### 정산 79초를 어떻게 숨기는가

정산창 앞에서 79초를 기다리게 할 수는 없다. **막지 않고 뒤에서 돌린다.**

```
하루 끝  →  정산창 즉시 표시 (점수는 이미 클라이언트에 있다)
         →  동시에 /api/settle 발사 (await 하지 않는다)
플레이어가 [Space]  →  다음 날 기상. 아직 안 와도 괜찮다
첫 이벤트 예약 시각까지 여유:  기상 09:00 → 첫 이벤트 15:20 = 게임 6시간 = 실제 3분
```

79초 ≪ 3분이므로 사실상 항상 늦지 않는다. 늦으면 그 이벤트만 건너뛴다 — 이벤트는
원래 거절 가능하고 무벌점이므로(§4) 빠져도 게임이 깨지지 않는다.

**플레이 중 실시간 LLM 호출은 `npc-reply`(자유 입력) 게임당 2~3회뿐이다.** 정체 이탈은
룰 기반이라 호출이 없다. 그 2~3번은 모두 연출상 정지해도 되는 결정적 장면이라,
"NPC가 잠깐 생각한다"로 읽히게 만들면 지연이 오히려 연출이 된다.

### 키가 **필요 없는** 것

시간·점수·Act 상태 머신 / 방 렌더링과 이동 / 상호작용과 정산창 /
정체 이탈 이벤트 / 붕괴 연출 / `src/room/*` 전부.
게임의 뼈대는 목 데이터로 끝까지 만들 수 있다.

### 비용 감각

Opus 5 = 입력 $5 / 출력 $25 per 1M tokens.
1회 완주(방 인식 1 + 정산 10일 + 대화 + 인증 1) 기준 대략 **$1 내외**.
시연 영상용 반복 플레이를 감안해도 한 자릿수 달러다. **비용은 이 프로젝트의 제약이 아니다.**

### 키 관리

- 발급: Anthropic Console → API Keys
- 서버(`.env`)에만 둔다. 클라이언트 번들·저장소에 절대 넣지 않음
- 모든 호출은 Node 프록시 경유 (파이프라인 3단계)

---

## 6. 미결정 (다음에 정할 것)

- **상태 수치(수면·스트레스·포만감)를 무엇에 쓸 것인가** — `src/game/vitals.js`에 그릇만 있다.
  지금은 표시만 하고 규칙에 영향이 없다. 붙일 때 **감점이 아니라 기회를 줄이는 쪽으로만** 쓸 것
  (§2 "감점 없음, 처벌 없음, 오직 가점만")
- **캐스팅의 `awake_window`와 게임 기상 시각이 어긋난다** — 디렉터는 `awake_window`를 보고
  편성하는데, 실제 기상은 `취침+9h`로 정해진다.
  실측에서 `10:30` 편성이 기상 `14:00`에 밀려 발화했다. 둘을 어떻게 맞출지 미정
- **배경 탭에서 게임 시간을 어떻게 할 것인가** — 브라우저는 숨겨진 탭의 `requestAnimationFrame`을
  멈춘다(4단계 구현 중 실측: 백그라운드 500ms 동안 0틱). 즉 **탭을 벗어나면 게임 시계가 선다.**
  시간이 이 게임의 핵심 자원이므로(§1) 그냥 두면 탭 전환으로 하루를 무한정 늘릴 수 있다.
  선택지: (a) 그대로 둔다 — 관대함 (b) `Date.now()` 기준으로 복귀 시 경과분 반영.
  시간 시스템 구현 시점에 결정
- **자유 입력 트리거 조건** — "결정적 장면"을 무엇으로 판정하는가.
  현재 후보: Act 2 진입(가설 `testing`) / 가설 `confirmed` 직전 / 붕괴 직전 (§4 참조)
- **이벤트 카탈로그** — 위 프레임에 맞춰 실제 이벤트를 채우는 작업. 이게 곧 이 게임의 밸런싱
- **일일 점수 기대치** — 정산 예시(+120, +140…)는 가상 수치. L0~L4 상호작용 개수가 정해져야 실제값이 나옴
- **심사위원 접근 경로** — 총 일수가 유동적이라 웹 빌드로 엔딩까지 보기 어려움. 시연 영상이 담당하되, 웹 빌드에 스킵/데모 모드가 필요한지
- **정체 감지 룰 파라미터** — 무행동 몇 초, 배회 판정 기준. 게임 길이를 가르는 값이 아니라 구현 중 손볼 튜닝값
