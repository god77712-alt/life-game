# 진행 기록

일자별 로그. 최신이 위. 작업 시작 전 맨 위 항목만 읽으면 현재 상태 파악 가능.

> 기록 규칙: 날짜별로 **한 일 / 결정한 것 / 다음** 세 줄만. 근거와 상세는 `DESIGN.md`로.

---

## 2026-08-01 (D-9)

### 한 일
- **집 구조 단순화 — 현관 맵 삭제.** `방 ─ 거실 ─(현관문)─ 골목 ┬ 편의점 / └ 공원`
  - 거실 아래쪽 문이 곧 현관문. **집에서 밖으로 나가는 길은 이것 하나, 잠기지 않는다** — 언제든 나갈 수 있다
  - 현관에만 있던 청소 대상(상자·빨래통)은 거실의 나가는 길목으로 옮김. 점수 기회를 잃지 않는다
  - 브라우저 실측: 방→거실→골목→편의점→골목→공원→골목→거실→방 왕복. `+30`×4 = 오늘 +120, 재방문 무점수
- **문 라벨을 도착지로 교체** — `RoomScene.destOf()` / `doorLine()`
  - 골목은 문이 셋인데 전부 "현관문"이라 어디가 어딘지 알 수 없었다. 이제 `편의점 [Space] 가기 L2 +30`
  - 오늘 이미 간 곳은 점수 표기를 뗀다 — 왔다 갔다 긁을 게 없다는 걸 라벨이 먼저 말한다
- **말풍선 진행 규칙 정리** — `src/ui/dialogue.js` 재작성. 원인은 셋이었다
  1. `showWaiting()`이 타자 타이머를 안 껐다 → 타이머가 계속 본문을 잘라 다시 썼다
  2. writer 실패 시 `close()`로 즉시 닫았다 — 크레딧 소진 중엔 매번 이 경로였다
  3. 그 수정이 `detail`을 통째로 출력해버려 **타자 연출이 사라졌다**
  - 규칙: **화면의 글은 플레이어가 키를 눌렀을 때만 바뀐다**
    ```
    타자 중     → 그 줄을 끝까지 (건너뛰기 아님)
    다 나온 뒤   → 다음 줄 / 없으면 선택지 / 선택지도 없으면 종료
    응답 대기 중 → `…`. 키가 안 먹는다. 도착해도 저 혼자 안 바뀌고 `▾ [Space]`로 알린다
    응답 실패    → 읽던 줄 그대로 두고 `[Space]`로 닫는다
    ```
  - `play(script, onDone, { pending: true })` — 뒷말이 올 예정이면 마지막 줄에서 안 닫는다
  - 브라우저 실측 4경로: 늦은 성공(3s) · 도착 후 키 진행 · 타자 중 실패 · 대기 중 실패 전부 정상
- **개발용 `?skip`** — 설문 9문항을 첫 보기로 채우고 바로 방으로. 답이 고정이라 캐스팅도 응답 캐시에 걸린다
- 테스트 90개 유지 (맵 연결·양방향·문 앞 통행·slot 검사 전부 통과)

### 결정한 것
- **현관은 공간이 아니라 문이다.** 아무 일도 일어나지 않는 방을 하나 더 두면 밖으로 나가는 길만 길어진다
- 문 라벨은 사물 이름이 아니라 **도착지**. 플레이어가 알아야 할 것은 "이게 문이다"가 아니라 "어디로 가느냐"다

### 컨텐츠 밀도 — 하루가 비어 있던 문제
실측으로 확인한 것: **DAY 1은 프롭 0 / 이벤트 0**, DAY 2~7도 이벤트 2건 + 프롭 4개(2곳뿐).
하루가 8실분인데 앞의 4분은 확실히 아무 일도 없었다. AI가 못 읽은 게 아니라 **읽을 게 없었다.**

- **`look` 행동 신설** — `src/game/ambient.js`. 점수 행동이 없는 물건은 **보는 것**이 행동이 된다
  - **점수를 주지 않는 게 핵심.** 점수를 주면 다 눌러보는 게 최적 전략이 되어 의미가 사라진다.
    점수가 없으면 **본 것은 보고 싶어서 본 것** → 순수 관측 신호
  - 무엇을 보고 무엇을 6일째 한 번도 안 보는가 = 회피의 지도
  - 상호작용 수: 내 방 5 → **9개**, 거실 2 → **13개**
- **새 type 5종** `fridge sink photo clock calendar` + 오브젝트 상한 10 → 14
  - `room-vision.md`에도 추가 — 원룸이면 냉장고·싱크대가 방 안에 있다
- **`ambient` — 디렉터가 오늘의 상태를 매일 새로 쓴다** (PLAN_SCHEMA + director.md)
  - 고정 문장만 돌려 쓰면 3일이면 다 읽힌다. 냉장고 안·TV 방영물은 그날의 연출이다
  - 문장 규칙: **판단 없이 본 것만.** "쓸쓸하다"가 아니라 "반찬통 두 개, 계란 세 알"
  - API 없거나 DAY 1이면 `ambient.js`의 준비된 문장 (날짜별로 돌아간다)
- **`POST /api/opening` — DAY 1 무대** (`server/index.mjs`)
  - 캐스팅 직후 발사. 분석 없이 편성만 1회. 목적은 검증이 아니라 **탐색**
  - ⚠ "탐색이다"라고만 쓰니 조심스럽게 굴다 **이벤트 0건**을 냈다 → 개수를 못박아 해결
  - 실측: 무대 3곳 9프롭(사람·동물·사물 섞임) / 이벤트 4건(14:40·17:20·19:40·22:10) / ambient 10개
    — 95초, $0.154. 방 사진 찍는 동안 도착한다
- 디렉터 규칙 조정: 공간 1~2곳 → **2~3곳**, 이벤트 1~2건 → **3~4건**
  (한 곳에만 몰면 플레이어가 거길 안 갔을 때 그날 관측이 통째로 0이 된다)

### 판정 AI(`referee`) 신설 — 자기 숙제 자기 채점 구조를 걷어냈다
문제: `analyst`가 가설을 세우면서 그 가설의 `confidence`·`verified_count`도 자기가 매겼다.
**반증이 구조적으로 불가능했다.**

- `prompts/referee.md` — 새 에이전트. 어제 디렉터가 **미리 쓴** `watch`(예측)와 오늘 결과를 대조
  - **현재 confidence를 입력에서 뺐다.** 0.65짜리를 보면 마저 올려주고 싶어지니까
  - `no_data`가 기본값. **증거의 부재는 부재의 증거가 아니다** — 안 간 공간은 회피가 아니다
  - `contradicted`는 3조건(기회 실재 · 예측이 지목 · 반대 관측)을 다 만족해야 한다
  - 기저율 규칙: 다 건드려보는 사람이 고양이를 건드린 건 '동물에 끌린다'의 증거가 아니다
- `hypothesis.mjs` — **confidence를 움직이는 곳은 여기뿐이다**
  ```
  supported     +0.10, 검증 +1     contradicted  −0.15 (반박이 더 무겁다)
  no_data        변화 없음          새 가설        START_CONFIDENCE 0.2
  ```
- `analyst`는 이제 점수를 안 매긴다 (스키마에서 필드 자체를 제거).
  대신 **statement를 좁히는 게 일**이다 — '동물을 좋아한다'는 반증 불가, '고양이에게 먹을 것을 챙겨준다'는 가능
- **채점과 분석을 병렬로.** 서로의 출력을 못 보는 게 맞다 — 독립적이어야 채점이 채점으로 남는다.
  직렬로 뒀더니 호출 3개가 이어져 fetch 5분 타임아웃에 걸렸다 (시뮬레이터는 `node:http`로 교체)
- 테스트 11개 추가 — 이동폭·클램프·이력·"analyst가 0.95를 보내도 무시된다"

**실측 (DAY 7~10, $2.34)**
- **DAY 9에 `confirmed` 도달** — `동물(고양이)에게 먹을 것 챙겨주기` conf 0.97 / 검증 7 / 3겹
- referee가 실제로 가설을 죽였다: `h1 정리/치우기`가 contradicted 3회로 0.38 → **0.08**.
  예전 구조라면 analyst가 계속 살려뒀을 가설이다
- confirmed 문장이 문턱의 모양까지 짚었다:
  > 대면으로 건네지는 것은 8.0초 앞에서 멈추고, 문고리에 걸어두는 방식만 "응"이 나온다
  → **골 맵 설계도가 여기서 나온다**

**같이 잡은 버그**: 신호가 매일 덮어써져 confirmed가 하루 만에 testing으로 풀렸다.
"3겹"은 하루에 세 겹이 아니라 **여태 세 겹 다 나왔다**는 뜻이므로 코드에서 누적하도록 수정.

### 골 맵 + 거울 — 엔딩을 만들었다
`confirmed`가 나오면 그 사람만의 마지막 장소가 생기고, 거기 **자기와 같은 모양의 사람**이 앉아 있다.

- **`goal-map` AI** — confirmed 가설 + 회피 패턴 + **플레이어가 직접 친 말 원문**으로 공간을 짓는다
  - 원칙: **문턱을 없앤 자리를 짓는다.** 설득하거나 훈련시키는 게 아니라 그 절차가 애초에 없는 곳
  - 실제 출력 (10일 시뮬레이션의 confirmed로 생성, 54초 / $0.084):
    > 계산대가 없고 값이 없다. 선반의 캔은 전부 같은 종류 한 가지뿐이라 **고를 일이 생기지 않는다.**
    > 사료 봉지는 이미 뜯겨 있고, 그릇 둘 중 하나는 누군가 벌써 채워놨다 — 시작하는 일은 남이 해놨다.
    > 달력에는 날짜 표시만 있고 이름 칸이 없다. **등록도 인수인계도 없다.**
    > 의자는 벽을 보게 놓여 있어 마주 앉는 구도가 만들어지지 않는다.
  - 분석가가 DAY 10에 찾아낸 문턱("오늘 피한 것은 '고르는 일'이었다")이 **공간 구조로 번역됐다**
- **`mirror` AI** — 거기 있는 사람. 이름이 아니라 상태로 부른다: `따지 않은 캔을 든 사람`
  - 플레이어보다 **조금 더 막혀 있다.** 같은 것을 원하고 같은 자리에서 멈춘다
  - 4턴 구조: 상황 → 문턱이 슬쩍 → 문턱을 말함 → **받되 해결하지 않음**
  - 금지: 플레이어 지목("당신도 그렇죠?") · 조언 · 고백 · 작별 인사
  - 실측 4턴 전문 확인. 3턴에서 문턱이 나왔다:
    > "저는 뜯는 걸 잘 못 해서. 두 개 중에 뭘 딸지 정하다 보면 그냥 시간이 가요."
    플레이어가 고른 말: **"아무거나 따도 얘는 먹을 텐데요"**
    → 남에게는 문턱을 낮춰준다. 자기한테는 못 하는 말이다. **이게 외재화다**
  - 마지막: "…그렇게 생각해본 적은 없는데." / "내일은 좀 일찍 와볼까." — **약속하지 않는다**
- **붕괴** — 거울을 만난 다음 기상부터 `scoreFor()`가 전부 0을 돌려준다.
  감점이 아니라 무득점. 처벌이 아니라 **게임이 더 이상 아무것도 세지 않는 것**이고 그 침묵이 메시지다
- 골목 아래쪽 벽은 여태 비어 있었다 — 거기 문이 하나 생기는 것이 Act 3의 시작

**잡은 버그 3개**
1. 모델이 `shelf`를 벽에, `calendar`를 바닥에 놨다 → 손으로 쓴 맵과 달리 **골 맵은 모델이 만든다.**
   `normalizeObject`를 export해 오브젝트 단위 교정을 거치게 함
2. 그 입력이 매퍼를 **크래시**시켰다 (`wallUsed[position]` undefined) →
   모델 출력이 게임을 멈추게 할 수는 없다. 모르는 벽 위치는 조용히 드롭
3. 거울 좌표를 손으로 박았더니 플레이어 시작 칸과 겹쳤다 → 골 맵은 매번 레이아웃이 달라지므로
   `mirrorSpot()`이 지을 때 찾는다 (빈 칸 · 시작 칸 아님 · 사방 2칸 이상 열림)

### 배포 준비 — GitHub + Render
- `git init` + 첫 커밋 (58개 파일). `.gitignore`가 `.env`·`.cache/`를 이미 막고 있어 키는 안 올라간다
- **정산 비동기화** — 정산 한 번이 95초~5분이라 HTTP 요청 하나로 붙들면
  **어느 호스트에 올려도 중간 프록시가 먼저 끊는다** (보통 100~120초)
  ```
  POST /api/settle   → 202 { job: "j..." }   ← 92ms에 돌아온다
  GET  /api/job/:id  → 202 running / 200 done  ← 4초 간격 폴링
  ```
  게임은 원래 정산을 안 기다린다(첫 이벤트가 몇 분 뒤) → 체감 변화 없음.
  가져간 job은 즉시 삭제 + 30분 TTL — 서버가 며칠 떠 있어도 메모리가 안 샌다
  실측: 접수 92ms → 폴링 17회 → 68초에 완료 → 재조회 `gone` ✓
- `render.yaml` — 저장소 연결하면 알아서 뜬다. **키는 `sync: false`**라 대시보드에서만 넣는다
  - 배포본은 `AI_CACHE=0`. 캐시가 걸리면 다른 플레이어에게 같은 결과가 나갈 수 있다
  - region: singapore (한국에서 가장 가깝다)
- `README.md` — AI 9개 역할표 + "세우는 쪽과 채점하는 쪽을 나눴다" 절 + 개인정보 처리
  (제출용 기술 문서의 뼈대가 그대로 여기 있다)

### 검토 도구 — "AI가 제 기능을 하는가"를 코드로 묻는다
- 지금까지는 **한 바퀴 돌려보고 로그를 읽는 것**뿐이었다. 그럴듯한 글은 읽으면 다 맞는 말 같다
- `scripts/players.mjs` — 성향이 **정반대인** 가짜 플레이어 2명. `--player=b`로 대조군
  - a: 살아있는 것 / 사람 회피 / 밤 · b: 사람이 하는 일 / 혼자 회피 / 낮
  - **b에게도 고양이와 밤 골목이 나오면 디렉터는 읽는 게 아니라 외운 것이다.** 이게 유일한 결정적 검사
- `sim-trace-<id>.jsonl` — 하루치 인과를 한 줄씩 (무대 → 반응 → 판정 → 다음 무대)
- `scripts/audit.mjs` — 추적 로그만 읽고 기계적으로 5가지를 검사. **API 호출 없음**
  1. 적응 — 지나친 건 빼고 다가간 건 이어가는가 2. 표적성 — 모자란 신호 겹을 노리는가
  3. 감쇠 — 증거 없는 가설이 내려가는가 4. 수렴 — 최고 가설이 좁혀지는가
  5. 정체 — 같은 프롭이 굳었는가 (반복 중에도 가설이 오르면 조여가는 것)
- `scripts/backfill-trace.mjs` — 이미 돌린 회차를 응답 캐시 + 실행 로그에서 복원 (일회성)

**돌려놓은 7일 감사 결과** — 무반응 제거 80% ✓ / 반응 추적 88% ✓ / 가설 지목 84% ✓ /
confidence 하강 5회 있음 ✓ (부풀리기만 하지 않는다) / 최고 가설 0.2 → 0.7 ✓

### 7일 시뮬레이션 결과 — **DAY 7에 `confirmed` 도달했었다**
- 실행은 "DAY 7 실패"로 끝났지만, 캐시에 남은 그날 analyst 응답을 판정에 걸어보니
  `h3 동물(고양이)에게 먹을 것 챙겨주기` **conf 0.70 / 검증 4회 / 3겹 전부** → `act 3, confirmed`
- **판정은 성공했는데 그 뒤 디렉터가 max_tokens에서 잘려 하루가 통째로 버려진 것.** 원인 2개, 둘 다 수정
  1. `director` maxTokens 6000 → 14000, `analyst` 8000 → 12000
  2. **`/api/settle`이 편성 실패로 판정을 버리지 않는다.** 무대는 다시 짜면 되지만
     그날의 판정은 그날 기록에서만 나온다. 이제 `plan: null, degraded: <사유>`로 200을 준다
  3. `readJson()`이 `stop_reason=max_tokens`를 잘림으로 명시 (전엔 "Unterminated string"만 보였다)

### 이전 실행 요약 (DAY 1–6)
- 디렉터가 스스로 좁혀 들어간 궤적: `정리` → `동물` → `동물(고양이)` → `고양이·먹이 챙기기`
  → `고양이에게 먹을 것 챙겨주기`. **7일이 현실적인 길이**
- 디렉터가 변수를 분리하는 실험을 설계한다 (DAY 7 무대의 reasoning):
  편의점에서 계산대 옆 고양이 캔을 지나친 뒤 → 다음 날 **공짜 참치캔을 거실 상 위에** 놓고
  편의점은 무대에서 뺐다. "막힌 게 구매 절차인가 접촉 자체인가"를 가르려는 배치다.
  같은 날 거실에 **잠든 엄마** — 사람은 있으나 마주치는 절차가 없는 유일한 배치로,
  4일 연속 미열람뿐이던 엄마 쪽 `reaction`(h3에 모자란 바로 그 겹)을 처음 얻으려 한 것
- 분석가가 발견한 회피는 "사람"이 아니라 **"사람과 마주치게 되는 절차"** — 초대가 붙은 연락만 미열람,
  고양이 얘기만 있는 연락은 59분 만에 열고 답함. 편의점은 물건이 계산대 옆이라 6분 만에 나옴
- 비용 **$1.92 / 6일** (in 73,170 · out 62,245 · 1049초)

### 공간마다 다른 결 — `THEMES`
- 어디를 가든 같은 마루·같은 벽이면 밖에 나온 느낌이 안 난다
- `src/render/textures.js`의 `THEMES` 4종: `indoor`(마루) `street`(젖은 아스팔트+담벼락 벽돌)
  `park`(잔디 — 타일 이음매를 지우고 풀결 점) `store`(흰 장판+형광등, 유일하게 밝다)
- **새 공간 = `THEMES` 한 항목 + `maps.js`의 `theme` 한 줄.** 골 맵도 이 경로로 들어온다
- 브라우저 실측: 방·거실 / 골목 / 편의점 / 공원 네 결이 전부 구분됨

### 다음 — 합의된 순서
1. ~~컨텐츠 밀도~~ ✅
2. ~~판정 AI(`referee`)~~ ✅
3. ~~거울 NPC + 골 맵~~ ✅
- **대조군 검사** — `--player=b`로 7일. 정반대 성향이 정반대 결론으로 가는가 (약 $2)
- **브라우저 실측** — 골 맵 도달 → 거울 4턴 → 붕괴까지 눈으로 확인 (확장 끊겨서 노드로만 검증함)
- 자유 입력(`npc-reply`) HTML input 왕복 연결
- **현실 인증(Act 4)** — 붕괴 다음 날 권한 요청 → 실제 이불 사진 → +100. 남은 "죽어도 지킬 것" 하나
- 대조군 검사 `--player=b`

---

## 2026-07-31 (D-10) — 2세션

### 한 일
- **파이프라인 2단계 완료** — `src/room/schema.js` (검증) + `src/room/mapper.js` (배치). 테스트 18개 통과
- `prompts/room-vision.md`의 그리드 매핑 절을 실제 구현에 맞춰 정정 (테두리 1칸 = 벽)
- **아트 디렉션 확정** → `ART.md` (레퍼런스: 바람의나라 / 미궁전설)
- **Claude API 호출 목록 정리** → `DESIGN.md` §5 — 키가 필요한 지점 6개 확정
- **플레이어 응답 방식 확정** (선택지 + 결정적 장면 자유 입력) → `DESIGN.md` §4
- **파이프라인 4단계 완료** — Phaser 조립. `index.html` + `src/main.js` + `src/scenes/RoomScene.js`
  + `src/render/textures.js`(플레이스홀더 텍스처) + `src/room/mock.js`(목 비전 4종)
  - 브라우저 실측 검증: 이동 · 침대/벽 충돌 · 문 진입 · 디버그 오버레이 · 밝기 전환 · 방 교체 전부 정상
- **상호작용 + 점수 레이어** — `src/game/actions.js`. **키 하나(`Space`)**, 시선이 대상을 정한다
  - 방 1 실측: 쓰레기·옷·컵 `+15`×3 → 이불 `+10` → 창문 `+15` → 문 `+30` = **오늘 +100 (방 최대와 일치)**
  - 청소하면 그 칸이 통행 가능으로 바뀌고 messiness가 줄어 방이 조금 밝아짐 (0.13→0.20)
  - 재시도 시 무반응·무점수 (하루 1회 규칙, 오브젝트 id 단위)
  - 대상 라벨 추가 — 행동 없는 사물도 이름을 띄운다 (`책상`). 플레이어를 안 가리게 위쪽 정렬
- **시간 시스템** — `src/game/clock.js` (순수 모듈). `DESIGN.md` §1 하루 길이 표를 테스트로 고정
  - 자정 강제 종료 · 취침 시각 → 익일 기상 시각 · 15시간 수렴까지 전부 검증
- **정산창 + HUD + 점수 팝업** — `src/ui/overlay.js`
  - HUD `DAY 1 14:32` / `오늘 +40 누적 40` + 하루 게이지(85% 넘으면 붉게)
  - 점수 팝업: 얻은 자리에서 `+15`가 22px 떠오르며 소멸 + 링 확산. **트윈 매니저 대신 씬 update()에서 직접**
  - 정산창: 행동 목록 · 오늘/누적 · `23:04 취침 → 내일 08:04 기상`. 붕괴 연출이 설 무대
- **침대 이중 행동** — 헝클어짐 → 이불 개기 / 개어짐 → 자기. 키 하나 제약을 상태로 푼 것
- **파이프라인 3단계 완료 (키 대기)** — `server/` 프록시. 정적 파일 + API를 한 서버에서 (CORS 없음, 배포도 1프로세스)
  - `server/prompts.mjs` — **프롬프트를 `prompts/*.md`에서 직접 읽는다.** 문서와 코드가 갈라질 수 없음
  - `server/claude.mjs` — `@anthropic-ai/sdk`, `claude-opus-5`, 구조화 출력(`output_config.format`)으로 JSON 보장
    - 스키마 enum은 `src/room/schema.js`를 그대로 재사용 — 클라이언트 검증과 어긋날 수 없음
    - 정책 거절 대비 서버측 폴백(`fallbacks: 'default'`) 기본 적용, 조직에서 불가하면 자동으로 폴백 없이 재시도
  - `src/photo.js` — 사진 선택 → 긴 변 1568로 축소 → 전송 → `loadVision()`. **저장하지 않음**
  - 키 없으면 사진 입력만 비활성화, 나머지 전부 정상 동작
- **API 키 확보 · 비전 파이프라인 실동작 확인** — 그린 방 이미지로 end-to-end 검증
  - 침대·창문·책상·모니터·의자·옷더미·컵·문을 정확히 읽어냄. **$0.025/장** (3603→284 tokens)
- **멀티 AI 구조로 전환** — `server/agents.mjs` 레지스트리 + 공통 실행기
  - `analyst`(분석) · `director`(편성) · `writer`(집필)로 분리. 프롬프트 3개 신규 작성
  - **분리 이유**: 분석은 스키마 고정·일관성, 생성은 스키마 최소·매번 달라야 함. 합치면 서로를 망침
  - `server/hypothesis.mjs` — **`confirmed` 3조건 판정을 코드가 한다.** 모델에 맡기면 상수가 흔들림
  - `/api/settle` = analyst → 코드 판정 → director → writer×N(병렬)
  - **실측: 79초 / $0.157 per 하루.** 10일 완주 ≈ $1.7
- **시작 설문 + 캐스팅 AI** — `IntroScene` · `src/ui/chooser.js` · `prompts/casting.md`
  - 4문항 문답(성별 / 실제 나이대 / 요즘 뭐 하나 / **몇 살의 나로 살아볼까**)
  - **설문은 무대만 세운다.** 욕망·회피는 안 묻는다 — 스키마에 그 필드가 아예 없음
  - 실측: 19세=재수학원·아빠·학원담임 / 26세=계약직 후·언니·과단톡 / 41세=어머니 69세·형·주민센터
  - `gap_note` — 실제 나이와 플레이 나이의 간극. **메모일 뿐 가설 아님** (단정 금지 프롬프트 작동 확인)
  - 캐스팅 33~46초는 **await 하지 않는다.** 방 진입까지 3ms, 사진 찍는 동안 뒤에서 완료
  - 🐛 잡은 버그: `fadeOut`이 내린 알파를 `ask`가 복구 안 해 **2번 질문부터 안 보임**
- **이벤트 파이프라인 게임에 배선** — `src/game/schedule.js` · `src/ui/dialogue.js`
  - 하루 끝 → `/api/settle` **발사만**(await 안 함) → 다음 날 예약 → 시각 되면 대화 재생
  - 선택 결과는 **원문 그대로** `dialogueLog`에 쌓여 다음 정산의 입력이 됨
  - 대화 중에는 시계도 멈춘다 — 읽는 속도가 자원이 되면 안 된다
  - 재생된 이벤트만 `history`로 넘겨 반복 편성을 막음
- **설문 4 → 7문항** — 접근 조건 3개 추가 (깨어 있는 때 / 나가는 구실 / 연락 확인 경로)
  - 디렉터가 이벤트 시각·통로를 **추측하지 않게** 한다. `confirmed`까지의 일수를 줄이는 최대 레버
  - 실측: `awake_window="실질 각성 15:00~06:00, 활동 집중 23:00~05:00"`,
    `outside_route="새벽 2~4시 도보 5분 편의점"`, `channel` 4경로 우선순위까지
- **설문 전면 재작성 (7 → 9문항)** — 의도를 숨긴 일상 질문으로
  - ~~"그래도 밖에 나갈 일이 있다면"~~ → **"집 근처에서 제일 자주 지나는 곳은"**
  - ~~"연락이 오면 그나마 확인하는 쪽은"~~ → **"휴대폰 알림은"**
  - **이유**: 전제가 보이면 방어적으로 답하고 시작 전부터 낙인이 찍힌다
  - 기질 문항 2개 추가 (혼자 있을 때 소리 / 영상 끝까지 보는 것) → `texture`
  - `texture`는 **디렉터에게만** 간다. 분석 AI는 안 본다 — 근거는 오직 행동
- **고정 맵 5개** — `src/game/maps.js` (거실 · 현관 · 골목 · 편의점 · 공원)
  - 손으로 쓴 vision JSON이라 **`buildRoom()`을 그대로 통과.** 매퍼가 하나면 된다
  - 매퍼에 `doorTiles[]` 추가 — 맵 하나에 문 여럿 (골목은 3개)
  - **점수는 문이 아니라 도착한 공간에.** 문에 붙이면 왔다 갔다 하며 긁을 수 있다
  - 실측: 5곳 각 +30 = 150, 재방문 0점, 도착 지점은 돌아오는 문 앞
- **플레이 피드백 반영 — 버그 3개** (2개는 제보, 1개는 그 과정에서 발견)
  - 🐛 방향키·Space가 페이지를 스크롤 → 두 씬에 `addCapture` (검증: `defaultPrevented=true`)
  - 🐛 **문이 하루 1회로 잠김** — `interact()`가 문을 `done`에 등록한 뒤 이동시켰다.
    이동은 점수 행동이 아니므로 하루 1회 규칙에서 제외. 문은 언제나 열린다
  - 🐛 **거실에서 자기 방으로 돌아갈 수 없었다** — `MAPS`에 `room`이 없어 `travel`이 거부.
    방은 사진 맵이라 별도 처리. 이제 방 ↔ 거실 자유 왕복
  - 점수 원칙 재확인: **문이 아니라 도착한 공간에, 공간마다 하루 1회.** 재방문 0점
- **플레이 피드백 2차 — 버그 4개** (전부 제보. 3개는 같은 뿌리)
  - 뿌리: 오브젝트 id(`trash_pile_1`)가 **맵마다 1번부터 다시** 붙는데 `done`은 맵 구분이 없었다.
    게다가 맵을 떠나면 새로 지어져 상태가 초기화됐다
  - 🐛 다른 방 쓰레기가 잠김 → `done` 키를 **`맵:오브젝트id`**로
  - 🐛 나갔다 오면 침대가 "오늘 완료" → 위와 같은 원인. + `mapState`로 상태 보존
  - 🐛 창문이 그 방만 밝힘 → **집 단위로 변경.** 하나 열면 집 전체가 다음 날까지 밝다
  - 🐛 가구가 문을 막을 수 있음 → 매퍼가 **문 앞~방 중앙 통로를 예약**.
    `large`는 4개 상한(초과분은 medium 강등) — 바닥이 가구로 덮이지 않게
- **이벤트 루프 실측 완료** — 실제 플레이 데이터로 끝까지 돌렸다
  - DAY 1 플레이(+175) → analyst 가설 3개 → director 편성 2건 → writer 대사·선택지
  - DAY 2 예약 시각에 대화창 자동 재생 → 선택 → `reads_as`와 함께 로그 기록
  - 실측 76.6초 / in=9932 out=4626
- **자정 = 취침이 아니게 분리** (설계 변경)
  - 자정 넘김 → **안 잔 것.** 00:00부터 하루 통째로. 이불 그대로라 '이불 개기' 없음
  - 침대 취침 → 취침+9h 기상. 짧아지지만 이불이 헝클어져 **+10 기회가 생긴다**
  - **취침은 20:00 이후만** — 없으면 14:00 취침 → 다음 날 1시간짜리 함정
- **상태 수치 그릇** — `src/game/vitals.js` (수면·스트레스·포만감). **표시만, 규칙 영향 없음**
- **테스트 도구** — DEV 패널 버튼 6개. 이벤트를 79초 기다리지 않고 볼 수 있다
  - 🐛 `playEvent`가 예약 없이 불리면 터짐 → `schedule?.` 로 가드
- **구조 전환 — 고정 NPC를 버리고 "매일 바뀌는 무대"로**
  - ❌ 되돌림: NPC를 좌표에 박고 idle 대사 하드코딩 (거실 엄마 / 편의점 점장 / 공원 아이)
  - ❌ 되돌림: `look` enum에 `mother|clerk|kid` — 인물을 미리 정해둔 것도 같은 실수
  - ✅ 맵은 **골격 + 빈 자리(slot)**만. 누가/무엇이 있는지는 디렉터가 매일 정한다
  - ✅ 디렉터 출력에 `scenes` 추가. 핵심은 `opening` — **다가가면 무슨 일이 벌어지나**
    대사는 미리 안 쓴다. 플레이어가 실제로 다가갔을 때 생성
  - ⚠️ 첫 시도에 디렉터가 7분 넘겨 죽음 → **하루에 공간 1~2곳만**으로 제한.
    비용뿐 아니라 설계상으로도 맞다 — 현실에서도 오늘 특별한 곳은 한두 군데
- **관측 장치** — `src/game/observer.js`. **분석 AI는 새로 안 만들었다**
  - `analyst`가 이미 그 일을 한다. 부족했던 건 AI가 아니라 데이터
  - `passed`(바라보고 지나침) · `unseen`(안 다가감) · `delay_min`(알림 응답 지연)
  - 실측: `쓰레기 2초 보고 치움 / 컵 3.4초 보고 지나침 / 창문·침대·옷더미 안 다가감`
  - **`reaction` 신호가 계속 비어 있던 원인**이 여기 있었다
- `npc-reply` AI 등록 + 대화창에 `직접 말하기…` 자유 입력 (배선은 미완)
- 🐛 프롬프트 중첩 코드펜스로 director 시스템 프롬프트가 잘림 → 추출기가 펜스 길이를 맞추도록
- **무대 렌더링** — 디렉터의 `scenes`가 실제로 화면에 놓인다
  - 프롭 스프라이트 3종(`person` · `animal` · `object`)만. 정체는 이름이 말한다
  - slot에 배치 + 통행 불가 → **다가가야 만난다**
  - `다가가기` → **`detail`을 먼저 띄우고 그 사이 writer 호출.** 읽는 동안 생성이 끝나 대기가 덮인다
  - 실측 11초, 대사 4줄 + 선택지 3개 + `직접 말하기…`
  - 무대도 `availableHere()`에 들어가 **지나친 것·안 다가간 것으로 기록**된다
- **디렉터 실호출 확인** — 새 프롬프트로 무대가 실제로 짜인다. 76~80초 / $0.12~0.13
  - 공간 2곳 · 프롭 4개 · 이벤트 2건. `"어느 쪽에 먼저 발이 멈추는지 비교"`
  - 어제 신호를 이어받음: `"체류 시간이 어제 고양이 40초를 넘는가"`
  - 🐛 **실호출로만 드러난 것 2개** — `look`에 묘사가, `signal`에 관찰 문장이 들어왔다.
    자유 문자열로 열어둔 탓. enum으로 되돌리고 관찰 기준은 `watch` 필드를 새로 팠다
  - 🐛 slot이 가구와 겹쳐 프롭이 **조용히 버려짐** (거실 a · 편의점 a). 좌표 수정 + 테스트 2개로 고정
- **연속 플레이 시뮬레이션** — `scripts/simulate.mjs`. 숨은 선호를 심고 AI가 찾는지 본다
  - DAY 1~3까지 진행 후 **`Connection error`로 중단** ($0.92 소모, 3일치 유실)
  - 회피 분석은 매우 정확: *"공간은 견디되 접촉은 피한다"*,
    *"응대할 사람을 기계로 치환하자 접근이 일어났다"*, *"가까운 쪽일수록 더 늦게 열고 더 짧게 답한다"*
  - DAY 3에 동물 가설 등장(conf 0.2). **`language`·`reaction`이 계속 병목** — 예상대로
  - 🐛 호출 1건 끊기면 하루가 통째로 날아감 → `maxRetries: 4` + 연결오류 2회 추가 재시도
- **비용 대응 (크레딧 소진)** — 개발 중 반복 호출이 실제 플레이보다 비싸다
  - `server/cache.mjs` — **응답 캐시.** 같은 입력이면 재호출 없음 → 개발 비용 0
    키에 프롬프트 본문·스키마·모델·effort가 전부 들어가 **한 글자만 고쳐도 자동 무효화**
  - 프롬프트 캐싱 — system 블록에 `cache_control`. 매일 같은 부분이 0.1배
- 테스트 90개 통과 (`npm test`), 서버 `npm start` → http://localhost:5173

### 결정한 것

| 항목 | 결정 |
|---|---|
| 그리드 구조 | 12×10 중 테두리 1칸은 벽, 바닥은 `x 1..10 / y 1..8` |
| 검증 위치 | 클라이언트. LLM 출력은 신뢰하지 않고 enum 통과분만 매퍼로 |
| 폴백 조건 | 유효 객체 2개 미만이면 통째로 `DEFAULT_ROOM` |
| bed/door | 없으면 주입. 10개 절단 시에도 생존 |
| cleanable | 프롬프트 값 무시, `type`으로 교정 (점수 기회 수를 LLM에 맡기지 않음) |
| 도달성 | spawn flood fill 후 **못 닿는 cleanable은 강제 이동** — L1 점수 소실 방지 |
| 디버그 뷰 | `renderAscii()` — 시연 영상용 겸 개발 확인용 |
| 아트 레퍼런스 | 바람의나라 / 미궁전설. 탑다운 배치 + 정면 스프라이트, 타일 스냅 4방향 |
| 타일 규격 | 32×32, 방 384×320, 표시 ×2 (768×640) |
| 밝기 | 연출이 아니라 **점수 피드백** — 커튼/창문 L1 행동이 화면을 바꾼다 |
| 모델 | 전 호출 `claude-opus-5`. 완주 1회 ≈ $1 — 비용은 제약이 아님 |
| 응답 방식 | **선택지 3개 기본 + 결정적 장면만 자유 입력** (게임당 2~3회) |
| 신호 | 선택지 = 정량(지연 0) / 자유 입력 = 정성(3겹). 원문은 자유 입력분만 보관 |
| 실시간 LLM | 게임당 2~3회(자유 입력)뿐. 나머지는 전부 정산(밤)에 몰림 |
| 캔버스 | 384×352 (위쪽 벽 2타일 렌더링분 +32) → ×2 = 768×704 |
| 텍스처 | **런타임 생성 플레이스홀더.** 타일셋 확보 시 `src/render/textures.js`만 교체 |
| 입력 | 큐 + 폴링 혼합 — 홀드는 연속 이동, 탭은 1칸 |
| 상호작용 | **키 하나(`Space`).** 바라보는 칸 → 없으면 발밑. type당 행동 1개 |
| 하루 1회 | 판정 단위는 **오브젝트 id** — 쓰레기 3개면 기회 3번 |
| 침대 | 상태로 행동이 갈린다. 헝클어짐 → 개기 +10 / 개어짐 → 자기. **자려면 개야 함** |
| 하루 리셋 | 날이 바뀌면 **방도 원상복구**. 점수만 누적 |
| 팝업 애니메이션 | 트윈 매니저 미사용 — 씬 `update(delta)`에서 직접. 검증 가능하게 |
| 필수 오브젝트 | bed · door · **window**(또는 curtain). 빛원이 없으면 밝기 설계가 죽음 |
| 대상 표시 | 테두리(노랑/초록) + 화살표 + **한 줄 라벨** `쓰레기 [Space] 치우기 +15` |
| 라벨 폰트 | 임시 시스템 monospace. 비트맵 한글 폰트 확보 시 `RoomScene.label` 스타일만 교체 |
| 시작 설문 | 9문항 문답. **무대만 세우고 가설은 세우지 않는다** (`DESIGN.md` §5) |
| 설문 원칙 | **의도가 보이면 안 된다.** 일상 질문으로 묻고 답이 신호가 되게 |
| 공간 | 방(사진) + 고정 5개. 도착 = L2 +30, 공간당 하루 1회. 재방문 0점 |
| 문답 UI | `src/ui/chooser.js` — 설문과 NPC 대화가 같은 컴포넌트 |
| 캐스팅 | 게임당 1회 · $0.05 · 33~46초. **await 안 함** — 방 사진 찍는 동안 완료 |

### 다음
1. **정산 → 디렉터 배선** — 하루 끝날 때 `/api/settle`을 **await 없이** 쏘고, 결과가 오면
   다음 날 예약 이벤트로. 79초는 첫 이벤트 시각(실제 ~3분)보다 짧아 안 막힌다 (`DESIGN.md` §5)
2. **Act 상태 머신** — `hypothesis.mjs`가 Act 번호를 이미 낸다. 그걸 게임에 물리면 됨
3. **비트맵 한글 폰트** — 정산창·라벨·대화창의 전제. 지금 레퍼런스에서 가장 어긋난 부분
4. 남은 AI 3개 — `npc-reply` · `goal-map` · `reality-check` (프롬프트만 쓰면 등록은 한 줄)

### 블로커
- 없음. API 키 확보 완료, 전 파이프라인 실동작 확인됨

### 아직 없는 것 (의도적)
- 실제 타일셋 · 비트맵 한글 폰트 — 플레이스홀더로 대체 중 (`ART.md` §6)
- Act 상태 머신 · 디렉터 · 붕괴 — 다음 단계
- L0 "물 마시기 / 씻기" — 대응하는 오브젝트 type이 스키마에 없음. 추가 여부 미정
- 배경 탭에서 시계가 멈추는 문제 — 시계는 만들었지만 대응은 미정 (`DESIGN.md` §6)

---

## 2026-07-31 (D-10) — 1세션

### 한 일
- 기획서(`CLAUDE_5.md`) 분석 — 게임 성격 규정, 핵심 기능 티어 분류, 리스크 5개 도출
- **코어 시스템 확정** → `DESIGN.md` 작성 (시간 · 점수 · Act · 디렉터)
- **비전 프롬프트 작성** → `prompts/room-vision.md` (파이프라인 1단계)
- `CLAUDE.md` 세팅 — 확정 상수를 세션 자동 로드 대상으로

### 결정한 것

| 항목 | 결정 |
|---|---|
| 시간 환산 | 1 실제초 = 2 게임분 |
| 기상 | 전날 취침 + 9시간 |
| 하루 종료 | 침대 취침 OR 게임 내 자정(강제) |
| 총 일수 | **고정 안 함** — 가설 `confirmed` 시점이 엔딩을 부름 |
| 점수 구조 | 일일 정산 + 누적 |
| 점수표 | 거리 차등 L0~L4 (`10→15→30→50→80`), 붕괴 `+0`, 현실 `+100` |
| Act 종속 | 날짜가 아니라 **가설 상태** |
| 임계값·하한 | **고정 안 함** — 이벤트로 유도 |
| 이벤트 성격 | **대화가 본체**, 3겹은 페이싱 레버. 믹스 비율은 디렉터 판단 |
| 이벤트 편성 | 하이브리드 — 대화·3겹은 정산 시 예약(LLM 1회), 정체 이탈은 실시간 룰 |

### 뒤집은 것 (재검토 시 주의)
- ~~"플레이 세션 안에서 엔딩까지"~~ → 엔딩은 세션 안에 들어갈 수 없음. 게임 내 1일 = 8분 기준
- ~~"총 일수 고정"~~ → 골 맵은 데이터가 쌓여야 열림. 일수는 결과값
- ~~"3겹 이벤트가 좋은 이벤트"~~ → 대화가 본체. 3겹은 늘어질 때 꺼내는 레버
- 기상 `+11h` → `+9h`

### 다음
1. **방 사진 파이프라인 2단계** — `position` → 12×10 그리드 매퍼 (순수 함수, API 키 불필요)
2. 3단계 Node 프록시 — **Claude API 키 필요**
3. 4단계 Phaser 타일맵 조립
4. 이벤트 카탈로그 초안 (코드 불필요, 병행 가능)

### 블로커
- **Claude API 키 미확보** — 3단계부터 필요. 그 전까지는 키 없이 되는 것(매퍼 / Phaser 렌더링 / 목 데이터 조립) 우선

---

## 파이프라인 상태판

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 비전 프롬프트 (사진 → 객체 JSON) | ✅ `prompts/room-vision.md` |
| 2 | `position` → 12×10 그리드 매퍼 | ✅ `src/room/{schema,mapper}.js` · 테스트 18 |
| 3 | Node 프록시 (키 은닉) | ✅ **실동작 확인** · 멀티 AI 레지스트리 |
| 4 | Phaser 타일맵 조립 | ✅ 렌더 · 이동 · 충돌 · 밝기 · 디버그 뷰 |

## 원 일정 (기획서 기준)

| 날짜 | 계획 | 실제 |
|---|---|---|
| 8/1~2 | 방 사진 파이프라인 | 7/31에 1단계 선행 |
| 8/3~4 | 방 상호작용 + 점수, 편의점 맵, 캐릭터 이동 | |
| 8/5~6 | 디렉터 루프 — 대화 이벤트, 가설 갱신 뷰, 골 맵 | |
| 8/7 | 붕괴 연출 + 권한 요청 + 현실 인증 1회 | |
| 8/8~9 | 시연 영상, 게임 소개서, AI 기술 문서 | |
| 8/10 | 제출 | |
