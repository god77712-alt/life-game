# 아트 디렉션 — 90년대 한국 2D 온라인 게임

레퍼런스: **바람의나라** (1996, 넥슨) / **미궁전설**.
"픽셀 게임"이 아니라 **그 시절 그 게임처럼 보이는 것**이 목표다.

> 아래 스펙은 바람의나라 쪽 특징에서 뽑았다. 미궁전설에서 따로 가져오고 싶은 요소가 있으면 이 문서에 추가할 것.

---

## 1. 시점 — 탑다운 격자 + 정면 스프라이트

레퍼런스의 핵심은 해상도가 아니라 **시점의 불일치**다.

```
배치는 위에서 본다        →  침대·책상·바닥은 탑다운 배치 (매퍼가 내보내는 그대로)
스프라이트는 정면을 본다  →  캐릭터·NPC는 카메라를 향한 정면 도트
```

3D적으로 틀린 시점이지만 90년대 한국 2D 온라인 게임의 인상은 여기서 나온다.
**`src/room/mapper.js`의 출력은 바꾸지 않는다** — 격자·충돌은 시점과 무관하다.

### 벽 렌더링

그리드상 벽은 1타일이지만, **위쪽 벽(y=0)은 화면에서 2타일 높이로 그린다** (위로 넘치게).
충돌 데이터는 그대로 1타일. 순수 렌더링 처리다.

---

## 2. 타일과 해상도

```
타일        32×32 px
방          12×10 타일        = 384×320 px
위쪽 벽 여유 +32 px            ← 벽을 2타일 높이로 세워 그리는 몫
캔버스      384×352  →  ×2  =  768×704
```

`ROOM_TOP = 32`. 격자 (x, y)의 화면 좌표는 `(x*32, 32 + y*32)`.
매퍼의 12×10 좌표계는 손대지 않고 렌더링에서만 아래로 민다.

- Phaser: `pixelArt: true`, `roundPixels: true`, `zoom: 2`. 보간 금지
- 고른 타일셋이 16×16이면 배율만 ×4로 올린다 (그리드 12×10은 유지)
- 384×320은 640×480 시절 화면비와 가깝고, 웹 빌드에서 스크롤 없이 들어간다

### 캐릭터 스케일

```
폭    1타일
높이  1.5 ~ 2타일   ← 타일보다 크게. 이게 90년대 한국 게임의 비율
발밑  1타일이 실제 점유 칸 (충돌 판정 기준)
```

머리가 뒤쪽 오브젝트를 가리는 게 정상이다. y좌표 기준 depth sort.

---

## 3. 이동 — 타일 스냅 4방향

자유 이동이 아니라 **격자 단위로 한 칸씩** 걷는다.

- 4방향(상하좌우). 대각선 없음
- 한 칸 이동에 고정 시간 **160ms**, 이동 중 입력은 큐에 하나만
- `collision[y][x] === 1`이면 그 칸으로 진입 불가. 막히면 **방향만 바뀐다**
- 입력은 **큐 + 폴링 혼합**: 누르고 있으면 연속 이동, 짧게 탭하면 keydown 큐로 1칸.
  폴링만 쓰면 프레임 사이에 끝난 탭이 씹힌다

`mapper.js`가 이미 12×10 충돌 그리드를 주므로 추가 물리 엔진이 필요 없다.
경직된 조작감은 결함이 아니라 레퍼런스의 일부다.

---

## 3-1. 상호작용 — 키는 하나

**행동 키는 `Space` 하나뿐이다. 무엇을 바라보고 있느냐가 행동을 정한다.**
메뉴도, 커서도, 행동별 단축키도 없다.

```
대상 결정   바라보는 칸의 오브젝트 → 없으면 발밑 칸
            (문처럼 밟고 설 수 있는 것을 위해 발밑도 본다)

침대 앞  Space → 이불 개기      L0 +10
쓰레기 앞 Space → 쓰레기 치우기  L1 +15
커튼 앞  Space → 커튼 걷기      L1 +15
창문 앞  Space → 창문 열기      L1 +15
문 앞    Space → 현관 나가기    L2 +30
```

- 카탈로그는 `src/game/actions.js`. **type 하나에 행동 하나** — 분기가 없어 새 오브젝트를 추가해도 규칙이 안 늘어난다
- 하루 1회 규칙은 **오브젝트 id 단위**. 쓰레기 더미가 3개면 기회도 3번
- 이미 한 행동은 반응하지 않는다 (감점도 없음 — `DESIGN.md` §2)

### 대상 표시

```
가능한 대상   노란 테두리 맥박 + 화살표 + 라벨
이미 한 대상  초록 테두리 + 라벨(✓)
행동 없는 사물 라벨만 (이름). 테두리·화살표 없음
대상 없음     아무 표시 없음
```

라벨은 대상 위에 뜨는 **한 줄**, 각진 베벨 상자.

```
쓰레기   [Space] 치우기 +15
창문     [Space] 열기 +15
침대     ✓ 오늘 완료
책상                        ← 행동 없는 사물은 이름만
```

- 어둠 오버레이보다 위에 그린다. 캄캄한 방에서도 읽혀야 한다
- **플레이어와 대상 중 위쪽에 맞춰 띄운다** — 아래를 보고 있을 때 캐릭터를 가리지 않게
- 좌우는 화면 밖으로 안 나가게 클램프

> 폰트는 지금 시스템 monospace다. **비트맵 한글 폰트가 들어오면 `RoomScene.label` 스타일만 교체**한다.
> 그 전까지는 이 라벨이 레퍼런스에서 가장 어긋나는 부분이다.

---

## 4. 색 — 어두운 실내에서 밝아진다

저채도 실내 톤에서 시작한다. 갈색·회색·탁한 청색. 검은 외곽선.

**이 게임에서 밝기는 분위기가 아니라 피드백이다.**

| 상태 | 화면 |
|---|---|
| 시작 (커튼 닫힘) | 전체 어둡게, 채도 낮음 |
| L1 청소 `+15` ×N | 치울 때마다 조금씩 밝아짐 |
| L1 커튼 걷기 `+15` | 방 전체 밝아짐 |
| L1 창문 열기 `+15` | 빛 + 바깥 소리 |
| 붕괴 이후 | 배경음 제거 (`DESIGN.md` §3) |

```
실외·편의점        항상 1.0
집 안 (방·거실·현관)
  창문/커튼 열기 전   0.20 - 0.10 × messiness     ← 청소하면 조금씩 밝아진다
  하나라도 열면       1.0                          ← 집 전체가. 다음 날 아침까지
```

**밝기는 방 단위가 아니라 집 단위다.** 내 방 창문을 열면 거실도 현관도 밝아진다.
방마다 따로 열게 하면 같은 행동을 세 번 시키는 꼴이고, 점수는 하루 1회뿐이라 보상도 없다.

다음 날이 오면 다시 닫힌다 — 아무것도 변하지 않는 반복(`DESIGN.md` §2).

> **window/curtain은 필수 오브젝트다.** 하나도 없으면 밝기를 올릴 수단이 사라져
> 이 설계가 통째로 죽는다. `schema.js`가 bed·door와 함께 주입한다.
점수 행동이 화면을 실제로 바꾸는 것이 **말 대신 시스템으로 전달한다**는 원칙과 맞는다.

---

## 3-2. 문답 UI — 설문과 대화가 같은 화면을 쓴다

시작 설문은 폼이 아니라 **문답**이다. 검은 화면에 질문이 한 글자씩 타자되고, 선택지가 아래에서 하나씩 떠오른다.

```
      성별을 물어도 될까.

        ▸ 남
          여
          말하고 싶지 않다

                        ↑↓ 이동   [Space] 선택
```

- `src/ui/chooser.js`. **NPC 대화도 이 컴포넌트를 그대로 쓴다** — 설문과 대화의 톤이 일치한다
- 타자 중 `Space` → 즉시 완성. 완성 후 `Space` → 확정. 게임 본편과 같은 한 키 규칙
- 숫자 입력 항목은 그 자리에서 `◂ 26 ▸`로 바뀌고 `←→`로 조절

마지막 문항 `몇 살의 나로 살아볼까.` 앞에서 **한 박자 쉰다**(900ms). 이 질문만 무게가 다르다.

---

## 4-1. 점수를 얻은 자리에서 숫자가 뜬다

무엇을 해서 몇 점을 얻었는지, 정산창까지 안 가도 알아야 한다.

```
얻은 자리에서   +15  숫자가 22px 위로 떠오르며 사라진다 (900ms, 감속)
같은 자리에서   노란 링이 한 번 퍼진다 (420ms)
색             #ffe27a + 어두운 외곽선 — 밝은 방에서도 어두운 방에서도 읽힌다
```

- 트윈 매니저를 쓰지 않고 씬 `update()`에서 직접 굴린다. 검증 가능하고 부품이 하나 준다
- 여러 개가 겹쳐도 각자 진행한다. 날이 바뀌거나 방이 갈리면 전부 정리

---

## 4-2. 시계 HUD와 정산창

### HUD (항상 떠 있음)

```
┌──────────────────┐                  ┌──────────────────┐
│ DAY 1   14:32    │                  │ 오늘 +40  누적 40 │
└──────────────────┘                  └──────────────────┘
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ← 하루 게이지
```

게이지는 기상 0에서 자정 1까지. **85%를 넘으면 붉게 변한다** — 숫자를 못 읽어도 하루가 끝나감을 알게.

### 정산창

```
        DAY 1  정산
  ────────────────────────
   L1 쓰레기 치우기   +15
   L1 창문 열기       +15
   L0 이불 개기       +10
  ────────────────────────
   오늘              +40
   누적               40

   23:04 취침 → 내일 08:04 기상
        [Space]
```

- 방을 덮는 어두운 판 + 각진 베벨 상자
- 아무것도 안 했으면 `아무것도 하지 않았다`
- 자정 강제 종료면 `자정이 지났다 → 내일 09:00 기상`
- **이 화면이 나중에 붕괴 연출이 서는 무대다** (`DESIGN.md` §2). 매일 보던 자리에 `오늘 +0`이 뜬다

---

## 5. UI와 폰트

- **비트맵 한글 폰트 필수.** 벡터 폰트를 픽셀 화면에 쓰면 레퍼런스가 즉시 깨진다
  - 후보: 둥근모꼴(DungGeunMo), 갈무리 계열 — **사용 전 라이선스 표기 조건 확인**
- 정산창·대화창은 두꺼운 베벨 프레임 + 각진 모서리. 둥근 모서리·그림자·반투명 금지
- 대화는 하단 고정 박스, 글자 한 자씩 출력(타자 효과)

정산창은 이 게임의 리듬 단위이자 붕괴 연출 무대다(`DESIGN.md` §2). 여기 UI에 제일 공들인다.

---

## 6. 에셋 조달 — 여기가 리스크

**바람의나라 스타일 무료 타일셋은 사실상 없다.** itch.io / OpenGameArt의 무료 실내 픽셀 타일셋은
대부분 밝고 둥근 현대풍이다. 그대로 쓰면 레퍼런스가 안 나온다.

대응:

1. 일반 픽셀 실내 타일셋을 받아 **채도·명도를 낮춰** 톤을 맞춘다 (Phaser tint 또는 이미지 전처리)
2. 레퍼런스는 타일 그림이 아니라 **시점 · 타일 스냅 이동 · 캐릭터 비율 · 비트맵 한글 폰트 · 베벨 UI**로 만든다
3. 라이선스 표기는 제출 문서에 반드시 포함

원본 타일 그림을 흉내 내는 데 시간을 쓰지 않는다. 위 5개가 인상의 대부분을 만든다.

---

## 7. 스코프 컷

| | |
|---|---|
| 죽어도 지킬 것 | 타일 스냅 이동, 커튼/창문 밝기 변화, 비트맵 한글 폰트, 정산창 UI |
| 잘라도 되는 것 | 걷기 애니메이션 프레임 수, NPC 다양성, 벽 2타일 렌더링, 타자 효과 |
